# CHANGELOG — TryBuy Backend

> Historical record of completed work. **NOT auto-loaded** by any agent entry point.
> Read on demand only when you need the history/rationale of a past change.
> Current state (overview, active tasks, known issues) lives in `snapshot.md`.

## Completed Milestones

- Upload delete ownership enforcement (backend-handoff Upload/media lifecycle
  item **a**, 2026-07-08, sweep): closed the FE-flagged 🔴 hole where any
  authenticated user could delete another user's Cloudinary media (post images /
  product images / avatars) by guessing/scraping a `public_id`. Enforcement is
  server-side in `UploadService.generateDeleteSignature` →
  `normalizeOwnedDeletePublicId`: the `public_id` must be `<allowed-folder>/${userId}_…`
  (leaf prefixed with the caller's JWT user id) unless the caller's `role` is
  `admin`; disallowed folder or malformed path → 400; foreign-owned leaf → 403 —
  all thrown **before** any Cloudinary `destroy` call (controller unit test proves
  Cloudinary is not hit on rejection). Ownership is authoritative because uploads
  are always named `${userId}_…` at signature time (`normalizeOwnedUploadPublicId`),
  so a user can neither sign nor delete media outside their own namespace. JWT
  `role` = `user.role?.rol_name ?? "user"` (admin = `"admin"`), so the admin bypass
  and the safe `"user"` default both hold. No migration; no response-shape change.
  Verified: `jest apps/gateway/src/upload` 14/14; live gateway (user 17) —
  DELETE `trybuy/posts/18_someoneelse` → 403, `trybuy/private/17_x` → 400,
  `17_nofolder` → 400, unauthenticated → 401. FE-facing result recorded in
  `frontend-handoff.md`. Follow-ups from the same handoff entry (b orphan cleanup
  on entity update, c signed upload constraints) recorded as SEC-M7/SEC-M8 — not
  done here (b is a >2-service feature, c is an FE-coordinated contract change).
- Chat/payments performance indexes (PERF-06, 2026-07-07, sweep): completed the
  pending live Aiven apply for the chat and payments lookup indexes. Node A MySQL
  applied `nodeA-20260707-003-add-chat-message-performance-indexes`
  (`database/add_chat_message_performance_indexes.sql`) for
  `messages(conversation_id, created_at)` using online
  `ALGORITHM=INPLACE, LOCK=NONE`. Node B PostgreSQL applied
  `nodeB-20260707-001-add-payments-order-id-index`
  (`database/add_payments_order_id_index.sql`) and
  `nodeB-20260707-002-add-payments-app-trans-id-index`
  (`database/add_payments_app_trans_id_index.sql`) using
  `CREATE INDEX CONCURRENTLY IF NOT EXISTS` on `payments(order_id)` and
  `payments(app_trans_id)`, each scoped to non-null values. The broad live status
  check showed unrelated pending manifest candidates, so the final dry-run,
  apply, and status verification used `--only` for the three PERF-06 ids. Validation:
  exact-ID dry-runs were clean (`runnable=1` for Node A, `runnable=2` for Node B),
  approved live applies completed, exact status checks report all three as applied,
  and `npx.cmd tsc --noEmit` passed. No API contract change and no FE handoff
  needed.

- Social performance indexes (PERF-05, 2026-07-07, sweep): added an idempotent
  Node A MySQL migration `database/add_social_performance_indexes.sql` for social
  read hot paths: `comments(post_id, created_at)`, `posts(is_hidden, created_at)`,
  and `posts(user_id, is_hidden, created_at)`, all using online
  `ALGORITHM=INPLACE, LOCK=NONE`. Added matching TypeORM `@Index` metadata to the
  `Post` and `Comment` entities. The migration is enabled in
  `database/migrations.manifest.json` as
  `nodeA-20260707-002-add-social-performance-indexes`. Also added
  `--only=<migration-id>` support to `scripts/migrate-database.mjs` so a single
  reviewed migration can be dry-run, status-checked, or applied without applying
  unrelated pending candidates. Validation: prettier/eslint clean on touched TS,
  `npx.cmd tsc --noEmit` clean, filtered dry-run showed exactly one runnable
  migration, and the approved live apply to Aiven Node A completed; filtered status
  now reports the migration as applied. No API contract change and no FE handoff
  needed.

- Wishlist / favorites (F6, 2026-07-07, sweep): added product-owned wishlist support
  for the storefront. New `wishlist_items` table/entity stores `{ user_id,
product_id, created_at }` with unique `(user_id, product_id)` and cascade cleanup
  when a product is deleted; SQL migration `database/create_wishlist_items_table.sql`
  is enabled in `database/migrations.manifest.json` as
  `nodeA-20260707-001-create-wishlist-items`. Product TCP patterns added:
  `product.wishlist.add`, `product.wishlist.remove`, and `product.wishlist.list`.
  Gateway endpoints added under `/api/products`: `GET /wishlist?page=&limit=`,
  `POST /wishlist/:productId`, and `DELETE /wishlist/:productId`. Add is
  idempotent (duplicate add still returns `isWishlisted:true`), delete is
  idempotent 204, missing/inactive product add returns 404, and list returns the
  standard paginated envelope with hydrated product cards plus `categoryIds` and
  `wishlistedAt`. Validation: prettier/eslint clean on touched TS, `npx.cmd tsc
--noEmit` clean, product service Jest spec passed 3/3, Node A migration dry-run
  includes the new enabled schema candidate, and live gateway self-test passed:
  login 201, cleanup delete 204, add 201, duplicate add 201, list 200 with product
  35 + `wishlistedAt` + `categoryIds`, remove 204, list no longer contains product
  35, missing product add 404. Storefront FE handoff recorded.
- Public profile email privacy (2026-07-07, sweep - closes SEC-H1): `GET /api/user/:id`
  and default `GET_USER_INFO` / `GET_USERS_BY_IDS` TCP calls now return only the
  public profile projection (`id`, `username`, `name`, `avatar`, `isActive`) and no
  `email`. Email is preserved only on private/admin paths that explicitly opt in:
  `GET /api/user/me`, admin user pagination, admin/GHN order buyer enrichment, and
  invoice generation. Product seller enrichment and social author decoration now use
  the public projection, and product enrichment no longer debug-logs the raw user
  object. Added focused regression coverage for user-service select shapes and
  gateway product seller enrichment dropping `email` even if an upstream user object
  contains it. Validation: prettier/eslint clean on touched TS files,
  `npx.cmd jest apps/user/src/user.service.spec.ts apps/gateway/src/product/product-ownership.service.spec.ts --runInBand`
  passed 11/11, and `npx.cmd tsc --noEmit` passed. Runtime self-test on the running
  gateway verified `GET /api/user/:id` omits email and social author enrichment omits
  email; admin orders returned no rows to assert buyer-email compatibility. Product
  list runtime verification was blocked because `product:3006` was offline
  (`502 connect ECONNREFUSED`), and starting the product service unsandboxed was not
  approved because it would connect a new process to shared Aiven infrastructure; the
  product enrichment behavior is covered by the new gateway regression tests. API
  context updated and storefront FE handoff recorded.
- Uploaded-media URL validation + upload rate limits + delete error codes (2026-07-07, upload API audit follow-up): closed the consumer-side gap that made the upload-signing hardening bypassable — every field storing an uploaded media URL was previously accepted as any string (`imageUrls` was only `@IsString`, post `imageUrls`/`videoUrl` were `@IsUrl` any-host, `avatar` was `@IsUrl` any-host), so a client could skip the upload flow entirely and persist an external/arbitrary URL (hotlink, dead link, or a non-image file rendered as media). Added a shared `@IsCloudinaryUrl({ folder, media }, { each? })` class-validator (`apps/gateway/src/common/validators/is-cloudinary-url.validator.ts`) that requires: `https:` + host exactly `res.cloudinary.com` (URL-parsed, so `res.cloudinary.com.evil.com` is rejected), our cloud name prefix when `CLOUDINARY_CLOUD_NAME` is set, the expected folder segment (`trybuy/products` / `trybuy/posts` / `avatars`), and a media-type file extension (images `jpg/jpeg/png/webp/gif/avif/bmp/heic/heif` — **svg excluded** as a script vector; video `mp4/mov/webm/mkv/avi/m4v`; extension-less Cloudinary auto-format URLs allowed). Applied to `create-product.dto.ts`/`update-product.dto.ts` (`imageUrls`, products folder), `social/dto/create-post.dto.ts` (`imageUrls` image + `videoUrl` video, posts folder), and `user/dto/user.dto.ts` (`avatar`, avatars folder). Also: `POST /api/upload/signature` gained `@RateLimit({ limit:60, ttl:60 })` and `DELETE /api/upload/media` `@RateLimit({ limit:30, ttl:60 })` (per-user via the existing Redis guard); and `deleteMedia` no longer swallows a non-ok Cloudinary response as `{ result:"not found" }` — an auth/quota/5xx now throws `BadGatewayException` (502) and a network failure `ServiceUnavailableException` (503), logged via `Logger` (Cloudinary returns HTTP 200 + `{result:"not found"}` for a genuinely missing asset, so a non-ok status is always a real upstream failure). Added `is-cloudinary-url.validator.spec.ts` (11 cases: valid, extension-less, wrong host, subdomain spoof, wrong folder, `.exe`, `.svg`, cloud-name enforcement, video field) and 2 new upload-controller specs (delete 502 on non-ok, 503 on network error). Validation: prettier/eslint clean on all touched files, `npx tsc --noEmit` clean, `npx jest apps/gateway` green (47/47 incl. the new specs), and live gateway self-test (shop `techstore_demo`): product create with non-Cloudinary `imageUrl` → 400; `.exe` under the right folder → 400; valid `trybuy/products/*.jpg` → no `imageUrls` error (passes validator); avatar in `trybuy/products` → 400; avatar in `avatars` → no `avatar` error. FE-facing result recorded in `../.agent-local/frontend-handoff.md` (Open).
- Upload signature/delete ownership hardening (2026-07-07, sweep - closes SEC-C2): `POST /api/upload/signature` now signs only allowlisted folders (`trybuy/products`, `trybuy/posts`, plus the existing storefront avatar folder `avatars`) and requires any client-supplied `publicId` to be a caller-owned basename (`${userId}_...`, no path); if omitted, the server still generates an owned id. The legacy storefront `userId` query param is still accepted but ignored; the JWT user id is authoritative. `DELETE /api/upload/media` now validates `public_id` as `<allowed-folder>/${userId}_...` before creating a Cloudinary destroy signature, with admin exempt from the owner-prefix check. Invalid folders/path-shaped IDs return 400; foreign prefixes return 403 before Cloudinary is called. Added focused upload controller/service tests for allowed folders, foreign upload IDs, delete ownership, admin delete bypass, and no-Cloudinary-call on rejected delete. Validation: prettier/eslint clean on upload TS files, `npx.cmd jest apps/gateway/src/upload/upload.controller.spec.ts apps/gateway/src/upload/upload.service.spec.ts --runInBand` passed 12/12, `npx.cmd tsc --noEmit` clean, and live gateway self-test passed: valid product/avatar signatures returned 201 with owned public IDs; legacy `userId` query still returned 201; disallowed folder 400; foreign upload publicId 403; path publicId 400; foreign delete 403; malformed delete public_id 400.
- Cart item ownership binding (2026-07-07, sweep - closes SEC-C1): `PATCH /api/cart/items/:id` and `DELETE /api/cart/items/:id` now bind the mutation to the authenticated user. Gateway cart routes pass `req.user.id` through the TCP payload, and Orders `CartService` loads `cartItem -> cart` before update/delete, returning `NotFoundException` when the item is missing or belongs to another user. Non-positive quantity updates now use the same owned removal path. Added focused unit coverage in `apps/orders/src/cart.service.spec.ts` for foreign update/delete rejection, owned update/delete success, and the `quantity <= 0` path. Validation: prettier/eslint clean on touched cart TS files, `npx.cmd jest apps/orders/src/cart.service.spec.ts --runInBand` passed 5/5, `npx.cmd tsc --noEmit` clean, and live gateway self-test passed: user B PATCH/DELETE against user A cart item returned 404 while quantity stayed 1; user A PATCH returned 200 and set quantity 2; user A DELETE returned 200 and removed the item.
- Notification preview length fix (2026-07-07, sweep - closes backend-handoff "Notification `preview` truncated to 20 chars"): social comment/reply events now send `preview` as the first 255 chars instead of `slice(0, 20)` (`apps/social/src/social.service.ts`), matching the storefront notification contract. `NotificationService.saveNotification` defensively caps persisted `message` and `preview` at 255 chars to match the existing `notifications` varchar columns, and the comment/reply notification `message` is kept generic so a full 255-char preview cannot overflow `message`. No schema change. Validation: prettier/eslint clean on touched TS files, `npx.cmd tsc --noEmit` clean, and live gateway/RabbitMQ self-test passed end-to-end: created post #13, comment #17, reply #18; `GET /api/notifications` returned exact full previews of 141 chars for `type:"comment"` and 134 chars for `type:"reply"` (both >20, both matching source text). FE-facing result recorded in `../.agent-local/frontend-handoff.md`; backend-handoff entry moved to Done.
- Unused-API sweep (2026-07-06): audited every gateway HTTP route against both frontends (storefront `../frontend`, GHN console `../web-flow-GHN`); unused-but-usable endpoints were revived+upgraded and handed to FE, unused-broken/duplicate/dangerous ones were deleted end-to-end. REVIVED: `GET /api/inventory/low-stock` — was public + unbounded + admin-console-less; now `@Roles("shop","admin")`, admin unscoped, shop auto-scoped server-side (gateway resolves the seller's productIds via `PRODUCT_MESSAGE_PATTERNS.GET_PRODUCT_IDS_BY_SELLER` first; empty → `[]` without hitting inventory), inventory `getLowStockItems` now filters `isActive`, accepts optional `productIds`, orders `availableStock ASC`, caps at 100 (`LOW_STOCK_MAX_RESULTS`) — closes PERF-13 and pairs with the existing shop-stats `lowStockCount`; FE already had `inventoryApi.getLowStock` declared unused, handoff entry written. REMOVED (kept deleted after the revive-or-delete re-review): gateway `GET /api/inventory` (unbounded findAll), `GET /api/inventory/sku/:sku`, `GET /api/inventory/:id`, `DELETE /api/inventory/:id`, `POST /api/inventory/check-stock` (dup of `GET /api/products/:id/stock-check`), `POST /api/inventory/reserve-stock` + `release-stock` (dangerous HTTP dups of the internal TCP checkout path — closes SEC-M3, resolves OQ-1); standalone SKU mutation routes `POST/PATCH/DELETE /api/products/:id/skus*` + product-service handlers (bypassed P0-05 SKU-diff/reference protection; canonical path is `PATCH /api/products/:id` with `skuList`; read `GET /api/products/:id/skus` KEPT); `GET /api/user/all` end-to-end (gateway route+service method, user TCP handler+service method, `GET_ALL_USERS` constant — superseded by paginated admin `GET /user?page=&limit=`; PERF-09/SEC-M2 user+inventory offenders gone, PERF-12 moot). BUGS FIXED DURING SWEEP: (1) inventory `findByProductId` returned an arbitrary row when a product had SKU rows — now selects the base row only (`productSkuId IsNull()` + `isActive`); (2) after removing `@Get("all")`, `/api/user/all` fell through to `@Get(":id")` with `+"all"`→NaN and leaked an arbitrary user (admin, incl. email/grants) — `ParseIntPipe` added to `GET /api/user/:id` (SEC-L1 instance) → now 400. Spec updated: `user.service.spec.ts` listing test now targets `getUsersPaginated` (SafeUser select, no password). Validation: `npx tsc --noEmit` clean, prettier+eslint clean on all 10 touched files, Jest green. Runtime self-test 11/11 on the live gateway: low-stock shop→200 `[]`, admin→200 4 rows (productIds 10/17/28/16, ASC), user→403; `/api/user/all`→400; `GET /api/inventory`→404; check-stock→404; reserve-stock→404; `GET /api/inventory/product/35`→200 base row; `GET /api/products/35/skus`→200; `POST /api/products/35/skus`→404; `GET /api/user/23`→200. FE handoff entry (low-stock contract + removed-route cleanup list) written to `../.agent-local/frontend-handoff.md`.

- VPS first-deploy runbook (2026-07-06): expanded `docs/deployment-runtime.md` into a first manual deploy runbook for the current NestJS + PM2 + Docker Compose production layout. The runbook covers Ubuntu installs for Node 22, Docker Engine/Compose, PM2, Nginx, Certbot, and Git; provider and host firewall checks; `/var/www/trybuy-api` app setup; production env file checklist; first manual deploy commands; PM2 startup/save; first-safe Nginx enable/Certbot/TLS reload sequence; `/live`, `/ready`, and `/health` checks; rollback notes; and future GitHub Actions CD secret inventory. Scope intentionally excludes GitHub Actions deploy, GHCR, Aiven connections, and migrations. No code/runtime changes.

- GitHub Actions CI for backend monorepo (2026-07-06): added `.github/workflows/ci.yml` for `pull_request` and `push` to `main` without deploy, image publishing, Aiven access, migrations, or secrets. CI uses Node 22 with npm cache keyed by `package-lock.json`, runs `npm ci`, non-mutating `npm run lint:check`, `npx tsc --noEmit`, full Jest unit/spec suite, `npm run build`, `node -c ecosystem.config.js`, `docker compose config`, and committed-diff whitespace checking. Workflow env uses safe dummy values only for config parsing/tests. Added `lint:check` because the existing `lint` script mutates files with `--fix`, and refreshed `apps/user/src/user.service.spec.ts` mocks for `UserAddressRepository`/`DataSource` so the existing suite is CI-ready. Local validation passed: lint check (7 pre-existing e2e warnings, exit 0), Jest 15/15 suites and 92/92 tests, `npx.cmd tsc --noEmit`, `npm.cmd run build`, `node -c ecosystem.config.js`, `docker compose config`, and `git diff --check`.

- Gateway payment callback facade hardening (2026-07-06): tightened the new gateway-owned `POST /zalopay/callback`, `POST /vnpay/callback`, and `GET /vnpay/callback` paths for production callback safety. Callback POST routes now explicitly return HTTP 200 instead of Nest's default 201, and all callback facade routes keep raw provider response bodies by using `SkipResponseWrap`. Added minimal gateway-side shape checks before TCP forwarding: ZaloPay requires non-empty `data` + `mac`; VNPay requires non-empty `vnp_TxnRef`, `vnp_TransactionNo`, and `vnp_SecureHash`. Invalid payloads return provider-shaped failure bodies without calling payments service. Added callback-specific `@RateLimit({ limit: 300, ttl: 60 })` using the existing Redis-backed gateway guard. Production nginx callback locations now cap request bodies at `64k` and proxy only to gateway `127.0.0.1:3000`. Internal TCP host constant changed to `127.0.0.1`, and payments HTTP callback listener now binds to `127.0.0.1` so it is loopback-only under PM2. Added controller/service focused tests covering raw response shapes, invalid-shape short-circuiting, HTTP 200 metadata, and rate-limit metadata. Validation: focused callback Jest specs, `npx.cmd tsc --noEmit`, `npm.cmd run build`, and `git diff --check` passed. No real provider calls, migrations, or CI/CD changes.

- Gateway-owned payment callback facade (2026-07-06): moved public ZaloPay/VNPay server callback exposure behind the gateway without changing provider response contracts. Added unprefixed public gateway facade endpoints `POST /zalopay/callback`, `POST /vnpay/callback`, and `GET /vnpay/callback`; they forward raw body/query payloads over TCP to payments service patterns `payment.zalopay_callback` / `payment.vnpay_callback`. The existing payments callback handlers still perform MAC/checksum validation before DB writes and keep the idempotent `completePayment` path, so duplicate callbacks remain no-op after the first `payment_completed` emit. Added a `SkipResponseWrap` decorator so provider callbacks return raw `{ return_code, return_message }` / `{ RspCode, Message }` rather than the gateway response envelope. Updated production nginx callback locations to proxy to `127.0.0.1:3000` instead of the payments HTTP app on `3007`. No provider calls, Aiven connections, migrations, service starts, or CI/CD changes. Validation: focused Jest callback facade spec passed; `npx.cmd tsc --noEmit`, `npm.cmd run build`, `node -c ecosystem.config.js`, `docker compose config`, and `git diff --check` passed.

- Social-notification metadata for deep-linkable comment/reply notifications (2026-07-06, sweep — closes backend-handoff "Notification comment/reply thiếu postId" + FE P-item): comment→post-owner and reply→comment-owner notifications now persist and return `postId`, `actorId`, and `preview` alongside `type:"comment"|"reply"` and `orderId:null`, so the storefront can deep-link `/post/:id` and render a personalized "X commented: <preview>" instead of the generic English string with the wrong id. The `Notification` entity (`apps/notification/src/entities/notification.entity.ts`) gained nullable `post_id`/`actor_id`/`preview` columns and widened `order_id` to `bigint`; `NotificationService.saveNotification` takes an optional metadata arg; the RMQ `comment_created`/`reply_created` consumers pass `{postId, actorId, preview}` (were dropped on save). Applied the idempotent, INFORMATION_SCHEMA-guarded migration `database/add_social_notification_metadata.sql` to Aiven — REQUIRED because notification runs `synchronize:false`, so without it every `saveNotification` INSERT (ALL types) would fail "Unknown column" once this code deploys. Validation: `npx.cmd tsc --noEmit` clean; live pre/post column probe confirmed the 3 columns + bigint; end-to-end runtime self-test on the running gateway (post #11 owner user 20, commenter user 17) — comment produced notif id 83 for user 20 `{type:"comment",orderId:null,postId:11,actorId:17,preview:"Great post from user"}`; reply produced notif id 84 for user 17 `{type:"reply",orderId:null,postId:11,actorId:20,preview:"Thanks user17, reply"}`; existing order notifications still return with `postId/actorId/preview` null (no regression). FE-facing result recorded in `frontend-handoff.md`.

- Production VPS runtime support (2026-07-05): prepared the single-VPS runtime split for AWS Lightsail/EC2. PM2 remains responsible for all 10 compiled NestJS apps (`gateway`, `orders`, `user`, `product`, `social`, `notification`, `chat`, `inventory`, `payments`, `rewards`) with per-service restart policy, memory restart limit, delayed restarts, and log files under `logs/`. Added PM2 helper scripts (`pm2:start`, `pm2:restart`, `pm2:stop`, `pm2:logs`, `pm2:save`) plus `deploy:check`. Reworked Docker Compose to infrastructure-only Redis + RabbitMQ with named volumes, loopback-bound ports, healthchecks, no NestJS/MySQL/PostgreSQL containers, no hardcoded RabbitMQ password, and optional profiled RedisInsight/Uptime Kuma services. Updated env examples and added `docs/deployment-runtime.md` documenting PM2 apps, Docker infra, external Aiven DBs, Nginx/Certbot, and VPS commands. Corrected nginx websocket proxy targets so `/socket.io/` routes to the gateway on `3000`, where the current `/chat` and `/notifications` Socket.IO namespaces live. No production services were started, no Aiven connection was opened, and no migrations were run. Validation: `node -c ecosystem.config.js`, `docker compose config`, `git diff --check`, `npx.cmd tsc --noEmit`, and `npm.cmd run build` passed; Docker config emitted only a user-level Docker config access warning while still returning exit 0.

- Gateway production security hardening (2026-07-05): added focused defensive gateway hardening without changing business logic. Gateway now uses explicit security headers middleware, request body limits, global validation with whitelist/transform/forbidNonWhitelisted and sanitized validation errors, shutdown hooks, production-disabled Swagger unless `SWAGGER_ENABLED=true`, stricter production CORS with no localhost fallback/wildcard credentials, env-aware HttpOnly auth cookie options, generic JWT failures, production-sanitized 500/auth error responses, and Redis rate limiting that fails closed in production but remains fail-open locally. `/live`, `/ready`, and `/health` remain public/unprefixed. `helmet` installation was attempted but blocked by dependency-change approval policy, so equivalent explicit headers were added instead.

- Migration manifest safety tightening (2026-07-05): reclassified the SQL runner as incremental-only production deploy tooling, not a fresh Aiven bootstrap path. Disabled/manual-only risky enabled candidates: `database/create_cart_items_table.sql` (does not match active CartItem write path), `database/create_product_skus_table.sql` (depends on base `products` outside enabled manifest), `database/create_shipping_history_table.sql` (depends on base `orders` outside enabled manifest), and `database/add_social_notification_metadata.sql` (already covered by `apps/notification/src/migrations/create_notifications_table.sql`). Runner output now warns in dry-run/status/apply that base schema must already exist; status mode documents that it creates `schema_migrations` if missing. Database README and agent context now warn that `CREATE TABLE IF NOT EXISTS` does not validate existing table shape.

- Explicit SQL migration runner (2026-07-05): added a small manifest-driven migration system for production-like Aiven deployments without touching business logic. New `database/migrations.manifest.json` documents Node A MySQL and Node B PostgreSQL SQL files, enabling only conservative idempotent schema candidates and blocking destructive/reset/seed/demo/cleanup/manual entries. New `scripts/migrate-database.mjs` supports `dry-run`, `status`, and `apply`, creates `schema_migrations` if missing, records id/filename/checksum/applied_at, refuses blocked categories, and requires `--confirm-production` for production apply. Added npm scripts `db:migrate:*`, migration SSL/env knobs, `database/README.md`, and refreshed database/architecture agent context to reflect external Aiven DBs plus Docker Redis/RabbitMQ. No migrations were run and no database connection was opened during implementation; validation used dry-run only.

- Gateway health endpoints (2026-07-04): added public unprefixed ops probes on the gateway only: `GET /live`, `GET /ready`, and `GET /health`. The new `apps/gateway/src/health` module does not reintroduce the removed `DatabaseHealthService`; it reports DB as `not_configured` because the gateway has no TypeORM `DataSource`, reports RabbitMQ as `not_checked` when configured because no safe existing connection-status provider exists, and checks Redis through the existing `CachedService` client via `PING` without opening a new connection per request. Responses expose only safe statuses and process metadata. The three routes are excluded from the global `/api` prefix for Docker/Nginx/Uptime Kuma/CI compatibility.

- Socket.IO websocket upgrade confirmation (2026-07-04, backend-handoff sweep): closed the FE-reported `/chat` polling-only handoff as backend-confirmed, no code change needed. Live gateway verification used a real user login cookie and `Origin: http://localhost:4173` against `http://localhost:3000`: `/chat` with default transports connected via `polling` and upgraded to final `websocket` (`upgrades:["websocket"]`); `/chat` with `transports:["websocket"]` connected directly on websocket; `/notifications` with default transports and websocket-only both connected on websocket. `npx.cmd tsc --noEmit` clean. Backend socket config remains `@WebSocketGateway({ namespace:"/chat" | "/notifications", cors: gatewayCorsOptions })` with dev CORS accepting localhost/127.0.0.1 on any port. FE handoff written so any remaining polling-only observation should be investigated in the FE runtime/proxy/browser state rather than backend CORS/adapter.

- Social legacy post image cleanup (2026-07-04): closed the FE-reported broken Cloudinary URL issue for old social posts. `SocialService` now sanitizes post read responses by removing only the known bad legacy URL markers (`/trybuy/posts/trybuy/posts/` and `/undefined_`) from `imageUrls`; valid image URLs are preserved, and all-broken legacy posts return `imageUrls: []`. Covered `GET /api/social/posts`, `GET /api/social/posts/:id`, `GET /api/social/posts/user/:userId`, and admin reported-post payloads. Added `database/cleanup_broken_social_post_image_urls.sql` to remove the same bad URLs from stored `posts.image_urls` for social DBs (`synchronize:false`). Verified: prettier/eslint clean for `apps/social/src/social.service.ts`, `npx.cmd tsc --noEmit` clean, runtime feed/detail/user-post checks all returned `BadUrlCount=0` while valid post #5 kept its image, and the cleanup SQL passed non-mutating `EXPLAIN UPDATE`; the SQL was not applied to the live DB because remote data mutation needs explicit approval.

- Featured sellers endpoint + F2 status-counts confirmation (2026-07-03, backend-handoff sweep): closed the 🔴 FE-blocking handoff item "feed right-rail needs a featured-sellers endpoint" — the RightRail card called admin-only `GET /api/user/all` and got `403` ×2 with console errors on every feed load. New `GET /api/user/featured-sellers?limit=<1..20>` (JwtAuthGuard, any authenticated role, `limit` default 5) → gateway `UserService.getFeaturedSellers` → TCP `{cmd:"user.featured_sellers"}` (`USER_MESSAGE_PATTERN.GET_FEATURED_SELLERS`) → user-service query builder: innerJoin on the shop role (query builder deliberately skips the eager `role` relation; `limit()` not `take()` because the N:1 join can't multiply rows and `take()`'s DISTINCT subquery hits the MySQL DISTINCT+ORDER BY trap — first attempt 500'd exactly there, same root cause as PERF-10), `isActive=true`, newest-first, selecting ONLY public fields `{id,username,name,avatar}`. Gateway route declared before `GET /user/:id` so the literal path wins. `GET /api/user/all` untouched (PERF-09 deprecate/bound note stands). Runtime self-test 5/5 (user-role cookie): default → 200 with shops 23/20 newest-first; `limit=1` → 1 row; `limit=0` → 400; unauth → 401. Files: `apps/gateway/src/user/{user.controller.ts,user.service.ts,dto/user.dto.ts}` (new `FeaturedSellersQueryDto`), `apps/user/src/{user.controller.ts,user.service.ts}`, `libs/constant/message-pattern.constant.ts`. Also closed the confirm-only handoff entry "status-counts F2 keys": `getStatusCountsByUser` zero-fills all `OrderStatus` values, which already include `return_requested`/`refunded` — verified live (buyer 17: `return_requested:0, refunded:1`). No migration. Both entries moved to Done in `../.agent-local/backend-handoff.md`; FE integration notes written to `../.agent-local/frontend-handoff.md`.
- PERF-10 — product `findAllProducts` split-query pagination (2026-07-03): the paginated product list (`apps/product/src/product.service.ts`, TCP `product.findAll` behind gateway `GET /api/products` + `/products/with-inventory/all`) previously `leftJoinAndSelect`ed brand + categories (ManyToMany) directly on the paginated builder, forcing TypeORM's distinct-subquery pagination path with cartesian row inflation per category. Fix: (1) the builder no longer joinAndSelects relations — categories are only `leftJoin`ed (no select) when a `categoryIds` filter is present; (2) pagination is now `getCount()` + a DISTINCT id-page (`select product.id` + `addSelect(product.<sortBy>) AS sortValue` — required because MySQL DISTINCT + ORDER BY needs the sort column in the SELECT list — raw `offset/limit`, ordered by `sortValue`); (3) the page of ids is hydrated via `repository.find({ where: { id: In(ids) }, relations: ["brand","categories"] })` and re-ordered to the id-page order via a `String(id)`-keyed map (product id is bigint → hydrates as string). Same `PaginatedResponse` shape. Behavior improvement: category-FILTERED products now hydrate their FULL `categories[]`/`categoryIds[]` — the old joinAndSelect truncated each product's categories to only the matched one (verified on multi-category products [20,21] and [15,21]). Companion gateway fix: `GetProductsQueryDto.categoryIds`/`brandIds` gained a scalar→array `@Transform` (undefined/null passthrough, else wrap-and-`map(Number)`), so a single `?categoryIds=18` now validates (previously 400 "categoryIds must be an array"); repeated keys `?categoryIds=16&categoryIds=18` remain the canonical multi-value syntax. Discovered during verification (recorded in snapshot Known Issues + FE handoff): bracket syntax `?categoryIds[]=` is whitelist-stripped by the global ValidationPipe (Express simple query parser → literal unknown key) → silently unfiltered, and the storefront FE has been sending singular `categoryId`/`brandId` (`frontend/src/api/products.ts:25-26`) so the marketplace category/brand filter never worked end-to-end — FE handoff entry written 2026-07-03 to rename the params. Validation: `tsc --noEmit` 0 errors, prettier/eslint clean. Runtime-verified: direct TCP probe `{categoryIds:[18]}` → total 2 (products 34/35, exact); HTTP single `?categoryIds=18` → the same 2; repeated keys 16+18 → exactly 5; single `?brandIds=2` on with-inventory → 4/4 brand-2; no-filter list unaffected (undefined passthrough); price-ASC pages disjoint + correctly ordered.
- PERF-08 — checkout `enrichOrderItems` parallelized + deduped (2026-07-03): the shared basket-pricing helper `enrichOrderItems` (`apps/gateway/src/order/order.service.ts`, on both the create-order and voucher-validate paths) previously, per SKU line, `await`ed `SKU_FIND_BY_ID` then `await`ed `PRODUCT_FIND_BY_ID` sequentially even though the product lookup only depends on `item.productId` (known upfront, not on the SKU response), and re-fetched the same productId once per line. Fix: (1) a `productPromiseById: Map<number, Promise<ProductPriceResponse>>` memo wraps `PRODUCT_FIND_BY_ID` so repeated productIds across items share one in-flight TCP send (`fetchProduct` helper — same `.pipe(timeout(10000))` + `MicroserviceErrorHandler.handleError` catch as before); (2) in the skuId branch the SKU send and `fetchProduct(item.productId)` now resolve via `Promise.all([skuPromise, fetchProduct(...)])` instead of two serial awaits; the non-skuId branch calls `fetchProduct` too. All post-fetch validations (`sku.isActive`, product/SKU mismatch, stock, `product.isActive`, base-price null) and their `BadRequestException` messages are unchanged; each fetch keeps its own timeout+error handler so any single item failure still aborts checkout with identical semantics. Response shape byte-identical. Per checkout: product TCP sends drop from N (one per line) to the count of distinct productIds, and each SKU line's two dependent calls now overlap. Validation: `tsc --noEmit` 0 errors, prettier/eslint clean. Runtime-verified 2026-07-03: base-price 2-line order same product 35 → order 114 (both priced 299, snapshot image captured); SKU 2-line order same product 50 (skus 23+24) → order 115 (priced 2000+3800=5800, skuLabel "Công suất: 65W"/"100W", tierIdx stringified `[0]`/`[1]`); SKU/product mismatch (sku 23 vs product 35) → 400 "SKU 23 does not belong to product 35" (error path unchanged).
- GAP-02 + PERF-03 — social feed Redis/DB N+1 fixed (2026-07-03): closed GAP-02 by adding `mget(keys: string[]): Promise<(string | null)[]>` to `CachedService` (`libs/cached/src/cached.service.ts`) — one Redis MGET round-trip, same-order results, `null` for missing keys, empty input short-circuits without hitting Redis. PERF-03: the identical per-post decoration block triplicated across `getPosts`/`getPostsByUser`/`getFollowingFeed` (`apps/social/src/social.service.ts`) — per post one Redis GET `post:like_count:*` (+`postLikeRepository.count` fallback + write-back), one `resolveIsLiked` (Redis GET + `findOne` fallback), and one `commentRepository.count` ≈ 2N Redis round-trips + N COUNT queries per page — replaced by a shared private `decoratePosts(posts, viewerUserId)` built on three batch resolvers: `resolveLikeCounts` (one MGET over `post:like_count:{id}`; misses → ONE `GROUP BY postId` count over `post_likes` with zero-fill + per-key `set` write-back), `resolveLikedPostIds` (no viewer → empty set without touching Redis; one MGET over `post:liked:{postId}:{viewerId}`; misses → ONE `find({postId: In(missing), userId})` + per-key "1"/"0" write-back), `resolveCommentCounts` (ONE `GROUP BY post_id` query-builder count over `comments`). Per page: Redis ops ≈2N→2 MGETs (plus write-backs only on cold cache), COUNT queries N+N→≤2. Cache-key formats and write-back values unchanged, so `likePost`/`unlikePost` `incr`/`decr` and the single-post `getPostById` path (which keeps `resolveIsLiked`) stay consistent. Response shape byte-identical (`Post & {likeCount,isLiked,commentCount}` in `PaginatedResponse`). Validation: `tsc --noEmit` 0 errors, prettier/eslint clean on both touched files; new helpers confirmed compiled into `dist/apps/social/main.js`. Runtime self-test 7/7 (canceltest1779978329 user 17, testuser_403 user 18): main feed 200 (post 6 `commentCount:1` via the GROUP BY path) → like post 8 → feed shows `{likeCount:1,isLiked:true}` for the liker → second viewer sees `{likeCount:1,isLiked:false}` (liked MGET-miss → DB fallback + write-back) → user-posts feed 200 → following feed 200 with real non-zero values (post 2 `likeCount:2,isLiked:true,commentCount:5`) → anonymous (no cookie) feed 200 all `isLiked:false` → unlike restores `likeCount:0` (state cleaned up). All 🔴 critical perf-audit items now closed. No FE impact (same shape) — no handoff entry needed.
- PERF-04 — orders MySQL hot-path indexes (2026-07-03): added `@Index` decorators to the two orders entities (previously zero indexes beyond PKs/FKs). `apps/orders/src/entity/order.entity.ts`: `idx_orders_user_id` (buyer list `GET /api/order/user/:id`), `idx_orders_seller_id` (seller list), `idx_orders_status_created_at` composite (analytics window scans + hourly stale-reservation sweeper, both filter status + created_at), `idx_orders_ghn_order_code` (GHN webhook `findOne` lookup). `apps/orders/src/entity/order_item.entity.ts`: `idx_order_items_seller_id` + `idx_order_items_product_id` (analytics joins / top-products `GROUP BY`); `order_id` intentionally NOT indexed — the `@ManyToOne`/`@JoinColumn` FK constraint already indexes it in MySQL. No SQL migration needed: orders runs TypeORM `synchronize:true`, so the watch-restart auto-created the indexes on Aiven. Validation: `tsc --noEmit` 0 errors, prettier/eslint clean; index names confirmed compiled into `dist/apps/orders/main.js`; orders service healthy post-restart (buyer list 200) — under `synchronize:true` a failed DDL crashes bootstrap before the TCP bind, so a clean boot implies the sync applied. Direct `information_schema.STATISTICS` verification + `EXPLAIN` spot-check were NOT run (direct Aiven query blocked by session permissions) — run them on the next authorized DB session if hard confirmation is wanted. Side-effect accepted per audit: marginally slower order writes. No FE impact.
- GAP-01 + PERF-02 + PERF-07 — batch product-by-ids TCP pattern + gateway N+1 fixes (2026-07-02): closed GAP-01 by adding `PRODUCT_FIND_BY_IDS: "product.findByIds"` (bare-string pattern, `libs/constant/message-pattern-product.constant.ts`) with a product-service handler (`product.controller.ts` → `ProductService.findProductsByIds`): TypeORM `find({where: {id: In(ids)}, relations: ["brand","categories","skus"]})` — same relations/shape as the per-id `findProductById`; **missing ids are skipped** (return found-only, no 404 — both gateway call sites already treated missing products as skippable), empty/non-array input → `[]` (never void, per TCP rule #5). PERF-02: `getProductsWithInventory` (`apps/gateway/src/product/product.service.ts`) rewritten from N per-id `getProductById` (each enriching its own user) + a second whole-list re-enrichment (≈3N TCP calls) to ONE batch product send ∥ the existing batch inventory send, then a single `enrichProductsWithUserInfo` pass + `attachCategoryIds` per product (P1-04 `categoryIds` preserved) → 3 TCP calls per request. Fixed a key-type mismatch found during self-test: PG bigint serializes `inventory.productId` as a string, so both the `inventoryMap` key and the product-side lookup are now normalized with `Number()` (the old code matched string-to-string implicitly). PERF-07: `buildProductMap` (`apps/gateway/src/order/order.service.ts`) rewritten from deduped per-id sends in `Promise.all` to one batch send keyed by `Number(product.id)`; empty id set short-circuits without a TCP call, and a batch failure degrades to an empty map (warn log) so order reads never fail on product enrichment — same skip semantics as before. Validation: `tsc --noEmit` 0 errors, prettier/eslint clean. Runtime self-test: `POST /api/products/with-inventory/multiple` `{productIds:[35,36,37,99999]}` → 3/3 real products with `user` + `categoryIds` + joined `inventory` (`availableStock`/`totalStock`/`isLowStock`), 99999 skipped; buyer list `GET /api/order/user/17` legacy (pre-snapshot) items decorated via the batch map (products with `imageUrls` → `image` populated; product 5 with `imageUrls:null` → correctly null); legacy order detail `GET /api/order/75` → live Cloudinary image resolved from the batch fetch. No FE impact (response shapes unchanged) — no handoff entry needed.
- PERF-01 — gateway product-list user-enrichment N+1 fixed (2026-07-02): `enrichProductsWithUserInfo` (`apps/gateway/src/product/product.service.ts`) previously sent one `{cmd: GET_USER_INFO}` TCP call per unique seller inside `Promise.all(userIds.map(...))` on every enriched product list read — N round-trips per request. Replaced with a single batched `{cmd: GET_USERS_BY_IDS}` send (`timeout(10000)` + `catchError → []`, preserving the graceful "enrichment failure returns products un-enriched" semantics), then the same `Map<number, UserData>` lookup. Shape verified identical: user service `getInfo` and `getUsersByIds` select the exact same columns (`id, username, email, name, avatar, isActive`), and the enriched `user` object keeps `{id, name, email, avatar}`. Call sites affected: `fetchProductsPage` (GET /api/products list) and `getProductsWithInventory` (POST /api/products/with-inventory/multiple) — search/by-category/by-brand/by-sku are raw pass-throughs that never enriched (unchanged). Validation: `tsc --noEmit` 0 errors, prettier/eslint clean. Runtime self-test on the live stack (watch-reloaded, new code confirmed in `dist/apps/gateway/main.js`): `GET /api/products?page=1&limit=5` → 5/5 items with populated `user`; `POST /api/products/with-inventory/multiple` `{productIds:[50,47]}` → 2/2 enriched + inventory present. No FE impact (response shape unchanged) — no handoff entry needed.
- F5 — Post moderation actions (2026-07-02): closed the roadmap gap where `POST /social/posts/:id/report` existed but admins had no way to review/resolve reports or take a post down. **RBAC (no DB migration):** added a new `post` resource with admin-only grants `read:any`/`update:any`/`delete:any` to `apps/user/src/rbac/grants.ts` — the gateway `RoleAuthGuard` reads the in-memory `accesscontrol` grants (NOT DB `rol_grants`), so a new resource needs no DB change. **DB migration (REQUIRED — social runs `synchronize:false`):** `database/add_moderation_to_social.sql` ALTERs `post_reports` (+`status` ENUM('pending','resolved','dismissed') DEFAULT 'pending', +`resolved_by` INT NULL, +`resolved_at` TIMESTAMP NULL, +idx `idx_post_reports_status`) and `posts` (+`is_hidden` TINYINT(1) DEFAULT 0, +`hidden_at` TIMESTAMP NULL); applied to Aiven 2026-07-02 via an idempotent existence-guarded script. Entities: `apps/social/src/entities/post-report.entity.ts` (+`status`/`resolvedBy`/`resolvedAt`) and `post.entity.ts` (+`isHidden`/`hiddenAt`). **Social service (`apps/social/src/social.service.ts`):** the 3 feed reads (`getPosts`, `getPostsByUser`, `getFollowingFeed`) now filter `isHidden:false`, and `getPostById` throws `NotFoundException` for a hidden post (so a hidden post 404s on public read). Five moderation methods: `listReportedPosts({status?,page,limit})` — groups reports by post via a `createQueryBuilder` (`COUNT`/`MAX(createdAt)` grouped by `postId`, offset/limit paged, distinct-post `total`), then batch-loads the posts + all their report rows and assembles each entry `{post, reportCount, pendingCount, latestReportedAt, reports[]}` using a typed `Map<number, PostReport[]>` (lodash groupBy was removed because it resolves to `any` under this project's type-aware eslint); `hidePost(postId, adminId)` sets `isHidden:true`/`hiddenAt` AND flips the post's `pending` reports → `resolved` (+`resolvedBy`/`resolvedAt`); `unhidePost(postId)`; `dismissReports(postId, adminId)` marks `pending` → `dismissed`, returns `{postId, dismissed:n}`; `adminDeletePost(postId)` removes the post + all its report rows in a `manager` transaction. All 404 if the post is missing. New TCP patterns `social_admin_{list_reported_posts,hide_post,unhide_post,dismiss_reports,delete_post}` (bare-string, `libs/constant/message-pattern.constant.ts`) + `social.controller.ts` `@MessagePattern` handlers. **Gateway:** `apps/gateway/src/social/social.service.ts` methods (standard `send`+`timeout(10000)`+`MicroserviceErrorHandler`; `listReportedPosts` enriches each `entry.post` with `author` via the existing `fetchAuthorMap`), new `SocialAdminController` (`@Controller("social/admin")`, registered in `social.module.ts`): `GET reports` (`@CheckPermission("post","read:any")`), `POST posts/:id/hide|unhide|dismiss` (`update:any`), `DELETE posts/:id` (`delete:any`), all `@UseGuards(JwtAuthGuard)`; query DTO `dto/reported-posts-query.dto.ts` (`status` `@IsIn`, `page`/`limit` `@Type(()=>Number)`+bounds). No route collision (`social/admin` vs `social/posts`). Validation: `tsc --noEmit` 0 errors, prettier/eslint clean on all 10 touched files. Runtime self-test 10/10 on the live stack (author `canceltest1779978329` id 17, reporter `testuser_403` id 18, 2nd reporter `techstore_demo` id 23, admin `testadmin` id 21): create post #10 (`isHidden:false` present → social recompiled) → report → admin `GET reports?status=pending` shows post 10 (`pendingCount:1`, author enriched, report `pending`) → hide → `{isHidden:true}` + public `GET /social/posts/10` → 404 + report auto-`resolved` + `hiddenAt` set → unhide → public GET → 200 → fresh report by user 23 → dismiss → `{dismissed:1}` → delete → `{success:true}` + GET → 404 + hide-on-deleted → 404 → non-admin GET/DELETE admin routes → 403. FE-facing (admin moderation queue + hidden posts drop from feeds) recorded in `.agent-local/frontend-handoff.md` (Open). No new test accounts created.
- F4 — Seller analytics dashboard (2026-07-01, read-only, NO migration): shipped the aggregation endpoints the seller storefront + the GHN shipping console (its one remaining FE contract) were waiting on. One orders TCP pattern `order.analytics` (`ORDER_MESSAGE_PATTERN.ANALYTICS`, bare-string) → `OrdersService.getAnalytics({sellerId:number|null, from?, to?, interval?, topN?})`; interfaces `AnalyticsQuery`/`RevenuePoint`/`TopProduct`/`OrderAnalytics` exported from `orders.service.ts` and re-used as the controller handler's payload/return types. `sellerId:number` → seller scope (filters `order.sellerId` + `oi.sellerId`; valid because TryBuy orders are single-seller — the gateway splits multi-seller carts), `sellerId:null` → global. Three aggregates built with `createQueryBuilder`+`getRawMany`: (1) **statusDistribution** — `GROUP BY order.status` over every order created in the window, zero-filled across all `OrderStatus` values (so pending/canceled/refunded stay visible), plus `totalOrders`; (2) **revenueOverTime** — `SUM(oi.price*oi.quantity)` + `COUNT(DISTINCT o.id)` over COMPLETED orders only, grouped by MySQL `DATE_FORMAT(o.createdAt, '%Y-%m-%d'|'%Y-%m')` per the `interval`, ascending; (3) **topProducts** — `GROUP BY oi.productId` top-N by quantity sold with revenue. `summary` = totalRevenue (sum of the series) / completedOrders / totalOrders / averageOrderValue (`round(totalRevenue/completedOrders)`). **Revenue = goods GMV only** (SUM price\*quantity over COMPLETED) — excludes shipping fee (paid to GHN) and voucher discount, and is consistent with topProducts revenue; all VND rounded to integer. Date window: `resolveAnalyticsRange` defaults to the last 30 days, snaps `from`→start-of-day and `to`→end-of-day inclusive, throws `BadRequestException` on an unparseable date or `from`>`to`. Gateway: DTO `apps/gateway/src/order/dto/analytics-query.dto.ts` (`from`/`to` `@IsDateString`, `interval` `@IsIn(["day","month"])`, `topN` `@IsInt @Min(1) @Max(50)`, all `@ApiPropertyOptional`, `declare` props); service `getSellerAnalytics(sellerId,query)` + `getShippingAnalytics(query)` share a private `fetchAnalytics` (`timeout(10000)`+`catchError`+`MicroserviceErrorHandler`); routes `GET /api/order/seller/analytics` (JwtAuthGuard, `req.user.id`) + `GET /api/order/admin/analytics` (`@CheckPermission("shipping","read:any")` — same gate as the GHN admin routes, so `logistics_operator`/`shipping_manager`/admin pass), both declared BEFORE `seller/:id` so the literal segment wins over the `ParseIntPipe` param route. Validation: `tsc --noEmit` 0 errors, prettier/eslint clean on all touched files (`orders.service.ts`, `orders.controller.ts`, gateway `order.service.ts`, `order.controller.ts`, new DTO, `message-pattern.constant.ts`). Runtime self-test 6/6: seller scope 200 (techstore_demo user 23, 6 orders → zero-filled statusDistribution `{pending:2,canceled:3,refunded:1}`, revenue 0 as no COMPLETED); global 200 (logistics_test, wide range monthly → totalRevenue 1,224,843, completedOrders 10, monthly series 2026-05/2026-06, top 3 products); invalid `from` → 400; `from`>`to` → 400 (`"from" must be on or before "to"`); shop→admin/analytics → 403; unauth seller/analytics → 401. FE contract recorded in `.agent-local/frontend-handoff-ghn.md` + `.agent-local/frontend-handoff.md` (Open). No new test accounts.
- Structured GHN address — master-data proxy + per-user address book + numeric order money fields (2026-07-01): closed the FE-blocking checkout-addressing epic (backend-handoff, 4 parts) that was the durable fix for the long-flagged best-effort free-text GHN resolution. **(Part 1) GHN master-data proxy** so the storefront can build Shopee-style cascading province/district/ward dropdowns that yield GHN codes: new gateway `ShippingModule` (`apps/gateway/src/shipping/{shipping.module,shipping.controller,shipping.service}.ts`) exposing `GET /api/shipping/provinces`, `GET /api/shipping/districts?provinceId=`, `GET /api/shipping/wards?districtId=` (all `JwtAuthGuard`, `timeout(10000)` + `MicroserviceErrorHandler`); wired to orders via new TCP patterns `order.shipping_{provinces,districts,wards}` (`libs/constant/message-pattern.constant.ts`) → `orders.controller.ts` handlers → new `GhnService.listProvinces/listDistricts/listWards` which reuse the existing 24h master-data cache and return `{ id, name }` where `id` is the GHN code (ProvinceID/DistrictID number, WardCode string); missing/invalid query param → 400. FE never touches the GHN token. Registered in `gateway.module.ts`. **(Part 2) Per-user address book**: new `UserAddress` entity (`apps/user/src/entity/user-address.entity.ts`, `@Entity("user_addresses")` `@Index(["userId"])` — display fields recipientName/phone/addressLine + GHN codes provinceId/provinceName/districtId/districtName/wardCode/wardName + isDefault, camelCase props → snake*case cols) + migration `database/create_user_addresses_table.sql`; added to `user.module.ts` forFeature. Microservice DTOs `apps/user/src/dto/user-address.dto.ts` (Create/Update, class-validator, `declare` props) + gateway DTOs `apps/gateway/src/user/dto/user-address.dto.ts` (same shape + `@ApiProperty`/`@ApiPropertyOptional`, examples ProvinceID 201 / DistrictID 1442 / WardCode "20101"). Service logic in `apps/user/src/user.service.ts` (all in `dataSource.transaction` to hold the single-default invariant): `listAddresses` (default-first, then newest), `createAddress` (first address forced default; `isDefault:true` demotes others), `updateAddress`/`setDefaultAddress` (userId-scoped findOne → 404 if not owned), `deleteAddress` (removing the default auto-promotes the newest remaining). New TCP patterns `user.address*{list,create,update,delete,set_default}`(USER patterns use the`{cmd:…}`wrapper) →`user.controller.ts`handlers → gateway`apps/gateway/src/user/user.service.ts`methods → gateway routes in`user.controller.ts`(declared BEFORE`@Get(":id")`): `GET/POST /api/user/me/addresses`, `PATCH /api/user/me/addresses/:addressId`, `PATCH .../:addressId/default`, `DELETE .../:addressId`(all`JwtAuthGuard`, scoped to `req.user.id`). Migration note: the user service runs TypeORM `synchronize:true`, so `user_addresses` auto-created on restart (confirmed live on Aiven — table present); the SQL file is for fresh/`synchronize:false`DBs. **(Part 3) Product weight**: no BE action — product reads already return the`weight`column at runtime; only the FE`Product`type needed it (see the prior product-money-fields entry). There is no per-SKU weight column yet. **(Part 4) Preview-vs-charged fee consistency**: the fee was already computed at`POST /api/order`via`previewShippingFee`on the same address+items and persisted — the real gap was TypeORM serializing DECIMAL as strings, so the fee returned by`GET /api/order/:id`(a string) didn't match the numeric preview. Applied the`decimalToNumber` transformer (`@app/common`) to `Order.total`/`codAmount`/`shippingFee`/`discountAmount` (`apps/orders/src/entity/order.entity.ts`), so order money fields now serialize as JSON numbers matching the preview. Validation: `tsc --noEmit`0 errors, prettier/eslint clean on all touched files. Runtime self-test 10/10 (buyer user 17): list empty → create #1 (forced`isDefault:true`) → create #2 (`false`) → set #2 default (→ #1 flips off, default-first order) → partial PATCH #1 → DELETE #2 (→ #1 auto-promoted default) → 404 on unknown id → 401 unauth → cross-user PATCH on #1 → 404 (ownership isolation via userId-scoped query); Part 4 confirmed on order 113 returning `total/codAmount/shippingFee/discountAmount`as numbers. FE-facing result recorded in`.agent-local/frontend-handoff.md`(Open); closes the epic in`.agent-local/backend-handoff.md`. No new test accounts created.
- Gateway CORS — single delegate, dev allows any localhost/127.0.0.1 origin, prod stays strict (2026-07-01): closed the FE-reported Socket.IO/HTTP CORS gap where the storefront served from `http://127.0.0.1:5174` got `No 'Access-Control-Allow-Origin' header is present`. Root cause: the allow-list was the comma-split `FRONTEND_URL` (default `http://localhost:5173`) re-derived independently in 3 places — HTTP `app.enableCors` (`main.ts`, `||` default), the `/chat` WS gateway, and the `/notifications` WS gateway (both `??` default) — so any origin not literally in the env (different host `127.0.0.1` and/or different port `5174`) was rejected. Fix: added one shared delegate `apps/gateway/src/common/cors.ts` exporting `gatewayCorsOptions` (a `CorsOptions` with an `origin` function + `credentials:true`) and `resolveAllowedOrigins()`/`corsOriginDelegate`, and wired all 3 call sites to it (`main.ts` `app.enableCors(gatewayCorsOptions)`; both `@WebSocketGateway({ cors: gatewayCorsOptions })`). The delegate allows: (a) requests with no Origin header (curl/server-to-server/same-origin), (b) any origin in the explicit `FRONTEND_URL` allow-list, and (c) **only when `NODE_ENV !== "production"`**, any `http(s)://localhost` or `http(s)://127.0.0.1` origin on any port (regex `^https?://(localhost|127\.0\.0\.1)(:\d+)?$`). In production the localhost bypass is OFF — only `FRONTEND_URL` passes — so this does not weaken prod. Rejection is silent (`callback(null, false)`, no thrown error → no log spam; the browser simply gets no ACAO header). Express `cors` and Socket.IO share the same `cors` package, so one delegate covers HTTP + both WS namespaces. No migration; `credentials:true` unchanged. **Canonical socket contract confirmed for FE:** socket base URL = the gateway origin / same port as HTTP (dev `http://localhost:3000`, NOT a separate `3010`); namespaces `/chat` + `/notifications`; connect with `withCredentials:true` (HttpOnly `access_token` cookie) or a JWT via `auth.token`/`?token=`; the notification env var name is FE-only (recommend `VITE_WS_NOTIFICATION_URL` → gateway origin, client appends `/notifications`). Validation: tsc (root + `apps/gateway/tsconfig.app.json`) 0 errors, eslint clean on all 4 files, prettier applied. Runtime (gateway dev-watch reloaded, `NODE_ENV=development`): HTTP `OPTIONS /api/user/login` → `localhost:5173` reflected, `127.0.0.1:5174` reflected, `evil.example.com` → no ACAO; Socket.IO handshake on `/chat` and `/notifications` → `127.0.0.1:5174` reflected, `evil.example.com` → no ACAO. FE-facing result recorded in `.agent-local/frontend-handoff.md` (Open); closes the Socket.IO CORS item in `.agent-local/backend-handoff.md`.
- Product money fields serialize as numbers — fix & runtime-verified (2026-07-01): closed the FE contract gap where product reads returned `DECIMAL` columns as strings (`"2000.00"`, `"39.00"`, `"299000.00"`) while the FE `Product`/`ProductSku` types declare them as `number`. Root cause: TypeORM/mysql2 serialize `DECIMAL`/`NUMERIC` as strings (the known gotcha in conventions). Fix: added a reusable TypeORM `ValueTransformer` `decimalToNumber` (`libs/common/src/transformers/decimal-to-number.transformer.ts`, exported from the `@app/common` barrel; `to` passes the JS number through, `from` coerces the decimal string → number, `null` preserved) and applied it via `transformer: decimalToNumber` to the decimal columns at the source: `Product.price`, `Product.rating`, `ProductSku.price`. Fixing it on the entity covers every product read path uniformly (list, by-id, by-sku, by-category, by-brand, search, `with-inventory/all`, `:id/with-inventory`, `with-inventory/multiple`, `/:id/skus`) plus the TCP payloads to the gateway — no gateway-side coercion needed. No migration; no DTO/shape change (only the runtime value type flips string→number). Internal consumers (orders authoritative pricing) were already defensive with `Number()`, so no regression. Verified: tsc/eslint clean, jest apps/product 3/3; live gateway (dev watch reloaded) — `GET /api/products/50/with-inventory` → `price 2000` (number), `rating 0` (number), `weight null`; `GET /api/products/50/skus` → `sku0.price 2000` (number); `GET /api/products/with-inventory/all` → `"price":2000`/`"rating":0` (were `"2000.00"`/`"0.00"`). Side note: product `weight` (grams) was already exposed on product reads as a plain `weight: number | null` column — so the related FE ask "return weight on product reads" is a FE-only type addition, not a backend gap; there is no per-SKU `weight` column yet. FE-facing note recorded in `frontend-handoff.md` (Open).

- F3 Voucher / coupon / discount codes — implemented & runtime-verified (2026-06-30): closed the roadmap gap where Rewards only had points and there was no discount-code mechanism. **Architectural decision (justified):** vouchers are OWNED by the Orders service (MySQL), NOT Rewards — discount validation, the order row, and the redemption record must commit in the SAME transaction as order creation for integrity, and Rewards has no TCP server to call synchronously at checkout. New entities `apps/orders/src/entity/voucher.entity.ts` (`@Entity("vouchers")` + `VoucherDiscountType` enum `percent|fixed`: `code` unique, `description`, `discountType`, `discountValue` DECIMAL, `minOrderAmount`/`maxDiscountAmount` DECIMAL nullable, `usageLimit`/`perUserLimit` int nullable, `usedCount` default 0, `isActive`, `startsAt`/`expiresAt` datetime nullable) + `apps/orders/src/entity/voucher-redemption.entity.ts` (`@Entity("voucher_redemptions")`: `voucherId`, `userId` bigint, `orderId`, `discountAmount` DECIMAL, `createdAt`); both added to `orders.module.ts` forRoot entities + forFeature. `order.entity.ts` gained `voucherCode` (col `voucher_code` varchar 64 nullable) + `discountAmount` (col `discount_amount` DECIMAL(12,2) nullable). Migration `database/create_vouchers_table.sql` (both tables + ALTER orders) — NOTE the orders service runs TypeORM `synchronize:true`, so the tables/columns auto-create on restart; the SQL file is for fresh/`synchronize:false` DBs. **Service logic (`apps/orders/src/orders.service.ts`):** `validateVoucherForCheckout(userId, rawCode, itemsTotal)` — uppercase-normalizes the code, 404 if missing/inactive, 400 on startsAt/expiresAt/minOrder/usageLimit-exhausted/perUserLimit violations, returns `{voucher, discountAmount}`; `computeDiscount` (percent → `itemsTotal*value/100` capped by `maxDiscountAmount`; fixed → value; both `Math.min(discount, itemsTotal)` + `Math.round`); `redeemVoucher(manager, voucher, userId, orderId, discountAmount)` — **atomic usage-cap** via conditional `UPDATE vouchers SET used_count = used_count+1 WHERE id=:id AND (usage_limit IS NULL OR used_count < usage_limit)`, throws `ConflictException` if `!affected`, then saves a `VoucherRedemption` (`discountAmount.toFixed(2)`); `previewVoucher` (returns `{code, discountType, discountAmount, itemsTotal, finalItemsTotal}`); admin `createVoucher` (409 dup, 400 percent must be 1–100 / fixed >0), `listVouchers` (PaginatedResponse, createdAt DESC), `deactivateVoucher` (404, sets isActive=false). `placeOrder` gained `voucherCode?: string | null` → validates, `total = itemsTotal - discountAmount + shippingFee`, persists `voucherCode`/`discountAmount` on the Order, and conditionally `redeemVoucher`s inside the same create tx after OrderItems save. New TCP patterns `order.voucher_{validate,create,list,deactivate}` (`libs/constant/message-pattern.constant.ts`) + `orders.controller.ts` handlers; `createOrder` payload now carries `voucherCode`. **Gateway:** new DTOs `apps/gateway/src/order/dto/voucher.dto.ts` (`ValidateVoucherDto`, `CreateVoucherDto`, `VouchersQueryDto`) + `voucherCode?` added to `create-order.dto.ts`; `order.service.ts` refactored item-pricing into a reusable `enrichOrderItems` (so checkout and voucher preview price the basket identically), rejects vouchers on multi-seller baskets (`BadRequestException`), and adds `validateVoucher`/`createVoucher`/`listVouchers`/`deactivateVoucher` (all `timeout(10000)` + `MicroserviceErrorHandler`); routes in `order.controller.ts`: buyer `POST /api/order/voucher/validate` (JwtAuthGuard, preview), admin `POST /api/order/admin/vouchers` (`@CheckPermission("order","create:any")`), `GET /api/order/admin/vouchers` (read:any), `PATCH /api/order/admin/vouchers/:id/deactivate` (update:any). Validation: `tsc --noEmit` 0 errors, prettier/eslint clean, `jest apps/orders/src/orders.service.spec.ts` green (spec constructor updated for the 2 new voucher repos at all 9 call sites). Runtime self-test (10/10) on the live stack — admin `testadmin`, buyer `canceltest1779978329` (id 17): voucher create (SALE10 percent, FLAT20K fixed) → 201; duplicate "sale10" → 409 (case-insensitive); invalid percent 150 → 400; list → 200 (paginated); preview SALE10 → discount 11960, finalItemsTotal 107640; preview below-min → 400; preview unknown → 404; checkout with PCT50 → order 113 total 299, `voucherCode:"PCT50"`, `discountAmount:"299.00"`, `codAmount:"299.00"`; 2nd order same user → 400 "already used voucher PCT50 the maximum number of times" (per-user limit); used_count incremented; deactivate FLAT20K → isActive false then validate → 404; deactivate missing 999 → 404; non-admin create → 403. Multi-seller voucher rejection is logic-only verified (all test products belong to seller 23). FE-facing (storefront checkout `voucherCode` + voucher preview + admin voucher CRUD) — recorded in `.agent-local/frontend-handoff.md`. No new test accounts created.
- F2 Buyer-initiated return/refund request — implemented & runtime-verified (2026-06-30): closed the roadmap gap where only admin/shipper could GHN-return and buyers had no way to request a return+refund. New `order_return_requests` entity/table (`apps/orders/src/entity/order-return-request.entity.ts` + migration `database/create_order_return_requests_table.sql`) with status enum `pending_review|approved|rejected` and refund-tracking columns (`refund_amount`, `refund_method`, `refund_status` `refunded|manual_pending`, `previous_order_status`, `reviewed_by`); `orders.status` enum extended with `return_requested` + `refunded` (the orders service runs TypeORM `synchronize:true`, so the table/enum auto-sync on restart — the SQL file is for fresh/`synchronize:false` DBs). **Lifecycle (orders service `apps/orders/src/orders.service.ts`):** `requestReturn(orderId,userId,reason)` — eligible only on the buyer's own order in DELIVERING/COMPLETED (else 400), dedupes any active request (`In([PENDING_REVIEW,APPROVED])` → 400), captures `previousOrderStatus`, flips the order to RETURN*REQUESTED, publishes `order.return_requested`. `reviewReturnRequest(requestId,reviewerId,reviewerRole,decision,rejectReason?)` — admin bypasses ownership, otherwise the reviewer must own a product in the order (reuses `getSellerProductIds` + `verifySellerOwnsOrder`); dispatches approve/reject. **approve** (private): best-effort `ghnService.returnShippingOrder` (failure non-fatal), release reserved stock (`releaseReservedItems(false)`), `refundAmount = Number(order.total)`, refund **recorded/simulated only — no real gateway call** (`refundStatus`: online vnpay/zalopay → `refunded`, COD → `manual_pending`), order → REFUNDED, publish `order.return_approved`. **reject** (private): requires `rejectReason`, restores `previousOrderStatus ?? COMPLETED`, publish `order.return_rejected`. Lists: `getUserReturnRequests(userId,page,limit)` (buyer's own, `PaginatedResponse.of`) and `getManagedReturnRequests(sellerId,isAdmin,page,limit,status?)` (admin sees all; seller scoped via subquery `rr.order_id IN (SELECT DISTINCT order_id FROM order_items WHERE product_id IN (:...productIds))`; optional `status` filter; newest-first). **Wiring:** 4 TCP patterns `order.return_request*{create,list*user,list_managed,review}` (`libs/constant/message-pattern.constant.ts`) + `orders.controller.ts`handlers; 3 fanout events`order.return*{requested,approved,rejected}` (`libs/common/src/constants/event.ts`) published via `publishOrderReturnEvent`(mirrors`publishOrderCanceledEvent`, ORDERS_EXCHANGE, `{data:{orderId},pattern}`); notification consumers (`apps/notification/src/notification.controller.ts`, 3 new `@EventPattern`handlers,`OrderInfo`extended with`sellerId`) → notify seller on requested, buyer on approved/rejected, each try/catch ack/nack (NotFound→no-requeue). **Gateway:** new DTOs `apps/gateway/src/order/dto/return-request.dto.ts` (`CreateReturnRequestDto`/`RejectReturnRequestDto`reason`@MaxLength(1000)`, `ReturnRequestsQueryDto`page/limit/status),`order.service.ts` methods (`timeout(10000)`+`MicroserviceErrorHandler`), and routes (`order.controller.ts`): buyer `POST /api/order/:id/return-request`, buyer `GET /api/order/return-requests/mine`, seller/admin `GET /api/order/return-requests`, `POST /api/order/return-requests/:id/{approve,reject}`(the literal`return-requests`GET routes are declared before`@Get(":id")`so the param route can't capture them). Decision (DEMO scope, decided per the user's instruction to choose and justify): refund is recorded+simulated rather than calling a real payment-gateway refund, and approve emits dedicated return events instead of`ORDER_CANCELED_EVENT`to avoid a misleading "order canceled" notification. Validation:`tsc --noEmit`0 errors, prettier/eslint clean,`jest apps/orders/src/orders.service.spec.ts`40/40 (spec constructor updated for the new`returnRequestRepository` injection). Runtime self-test (7/7) on the live stack — buyer 17 (`canceltest1779978329`), seller techstore_demo (id 23, owns products 47/50), admin testadmin (id 21): T1 request on completed order 112 → 201 (request id 1, `pending_review`, order→`return_requested`); T2 duplicate → 400 (not eligible, already `return_requested`); T2b request on canceled order 111 → 400; T3 buyer list-mine → 200 paginated; T4 seller managed list → 200 (sees order 112 via product ownership); T4b admin managed list `status=pending_review` → 200; T5 seller approves → 201 (`approved`, `refundAmount:6800`, `refundMethod:zalopay`, `refundStatus:refunded`, `reviewedBy:23`), order 112→`refunded`; T7 admin rejects a fresh request on order 107 with reason → 201 (`rejected`, reason persisted, `reviewedBy:21`), order 107 restored to `completed`. FE-facing (storefront order-detail return UI) — recorded in `.agent-local/frontend-handoff.md`. No new test accounts created.
- F1 Product reviews & ratings — verified & closed (2026-06-30): the roadmap listed F1 as the recommended next feature, but a codebase check found it was **already fully implemented and committed** in `3ceb9f6` (chore: review migration) + `984ff2b` (feat: reviews + approval enforcement), reachable from HEAD — the 2026-06-30 roadmap entry was simply stale. Verified the whole stack end-to-end: `product_reviews` entity (`apps/product/src/entity/product-review.entity.ts`, unique `(product_id,user_id)`) + migration `database/create_product_reviews_table.sql`; product service `createReview`/`deleteReview`/`findReviewsByProduct` + `recalculateProductRating` (AVG/COUNT → `product.rating`/`ratingCount`); TCP patterns `product.review.create|delete|findByProduct`; gateway routes `GET /api/products/:id/reviews` (public, paginated), `POST /api/products/:id/reviews` (JWT), `DELETE /api/products/reviews/:reviewId` (JWT, owner-only). The anti-fake gate lives in the gateway: `createProductReview` first calls `order.verify_product_purchased` → orders service `verifyUserPurchasedProduct` (`apps/orders/src/orders.service.ts`) which innerJoins order.items and requires an order with `status = COMPLETED` containing the product, else 404. `tsc --noEmit` 0 errors; migration confirmed already applied on Aiven. Runtime self-test (8/8) on the live stack using buyer 17 (`canceltest1779978329`, who has COMPLETED order #67 for product 27) and non-buyer 18 (`testuser_403`): T1 create as verified buyer → 201 (review id 8); T2 duplicate → 409 "Already reviewed this product"; T3 public list → 200 (review present); T4 `GET /api/products/27` → `rating:5.00, ratingCount:1`; T5 non-buyer create → 404 "Product not found in any completed order"; T6 buyer 18 delete buyer 17's review → 403 "Not your review"; T7 owner delete → 204; T8 aggregate recalculated back to `rating:0.00, ratingCount:0`. Test state left clean (review removed). No code change this session — verification + handoff bookkeeping only. FE-facing (storefront catalog/reviews) — recorded in `.agent-local/frontend-handoff.md`. One latent note: `product-review.entity.ts` types `product_id` as `int` while the migration and `products.id` are `bigint` — harmless at current id ranges, not fixed to keep the diff zero.
- GHN detail reflects demo-driven status in demo mode — acts like a webhook (2026-06-30): closed the last open backend-handoff item. After a successful `POST /api/order/admin/ghn/orders/:id/demo-status`, the detail endpoint `GET /api/order/admin/ghn/orders/:id` still re-fetched **live GHN** `getOrderDetail`, which returns the unchanged sandbox status `ghnDetail.status:"ready_to_pick"` (the GHN dev sandbox never advances a waybill) — so the console's GHN status badge stayed stuck even though the local status, `shipping_history`, and list `lastGhnStatus` all moved. Fix (approach b) in `apps/orders/src/orders.service.ts` `getAdminGhnOrderDetail`: the latest `shipping_history` row is now loaded **before** the live GHN fetch, and when `GHN_DEMO_ENDPOINTS_ENABLED === "true"` AND that latest row is a `demo_status` with a non-null `ghnStatus`, the demo status is made authoritative for `ghnDetail.status` via the new private `buildDemoGhnDetail(liveDetail, orderCode, demoStatus)` — it overlays only `.status` onto the still-fetched live detail (so the real receiver/COD fields are preserved) or synthesizes a minimal `GhnOrderDetail` if the live fetch returned null/errored, and clears `ghnDetailError`. **Outside demo mode the live GHN `getOrderDetail.status` wins unchanged** (flag-gated, so prod is untouched). No DB migration — reuses the existing `demo_status` history row the demo endpoint already writes. Validation: prettier/eslint clean, `tsc --noEmit` 0 errors, `jest apps/orders/src/orders.service.spec.ts` 40/40 (3 new `getAdminGhnOrderDetail` cases: override applied + real receiver field preserved when demo mode on; live wins when flag off; live wins when the latest history is a real `sync` not `demo_status`). Runtime self-test (shipmgr_test, `shipping update:any`, demo flag on) on order #62 (`LXD6YV`): after `POST .../demo-status {ghnStatus:"delivering"}`, `GET .../62` returned `ghnDetail.status:"delivering"` (was `ready_to_pick`), `ghnDetailError:null`, real `toName:"Nguyen Van QA"` preserved, `lastGhnStatus:"delivering"`. FE-facing result recorded in `.agent-local/frontend-handoff-ghn.md` (Open); closes the matching item in `.agent-local/backend-handoff.md`.
- GHN demo-status endpoint — drive the local lifecycle end-to-end for demos (roadmap #6) (2026-06-29): added a **demo-only** endpoint so the GHN shipping console can demonstrate the full `picking → delivering → delivered` (+ cancel/return) lifecycle on a sandbox order the GHN dev API never advances past `ready_to_pick`. New endpoint `POST /api/order/admin/ghn/orders/:id/demo-status` (`@CheckPermission("shipping","update:any")`) accepts `{ ghnStatus }` (enum `ready_to_pick|picking|delivering|delivered|delivery_fail|waiting_to_return|returned|cancelled`), simulates the GHN-side status **without calling real GHN**, and maps it through the **same** `mapGhnStatus`/`applyGhnStatus` path the real sync/webhook uses — so all forward-only/terminal-order/stock-release/event side-effects are identical to a real status change. It writes a `shipping_history` row and persists `lastGhnStatus`/`lastSyncedAt`, then returns the exact sync shape `201 {orderId, previousStatus, newStatus, ghnStatus, syncedAt}` (FE reuses `ShipmentSyncView` with zero new adapter). New TCP pattern `order.admin_ghn_demo_status` (`libs/constant/message-pattern.constant.ts`); `apps/orders/src/orders.service.ts` `setDemoGhnStatus(orderId, actorId, ghnStatus)` (404 if order missing) reuses `ShippingHistoryType.MANUAL_SYNC` with `action:"demo_status"` + `payloadSummary.demo:true` → **no DB migration**; `orders.controller.ts` TCP handler; gateway `order.service.ts` (`timeout(10000)` + `MicroserviceErrorHandler`), gateway route in `order.controller.ts`, and gateway DTO `SetGhnDemoStatusDto` (+ exported `DEMO_GHN_STATUSES`, `@IsIn`) in `dto/admin-ghn-update.dto.ts`. Also broadened `mapGhnStatus`'s cancel branch from `=== "cancel"` to `.includes("cancel")` so the FE enum value `cancelled` resolves to CANCELED (matching the existing `.includes("return")`). **Safety:** the whole endpoint is gated behind env flag `GHN_DEMO_ENDPOINTS_ENABLED` — `setDemoGhnStatus` throws `ForbiddenException("GHN demo status endpoint is disabled")` unless `process.env.GHN_DEMO_ENDPOINTS_ENABLED === "true"`, so it is inert in production (flag absent/false → 403) and never touches the real GHN API. Validation: prettier/eslint clean, `tsc --noEmit` 0 errors, `jest apps/orders` 37/37. Runtime wiring self-test (shipmgr_test, `shipping update:any`) on order #62: invalid `{"ghnStatus":"bogus"}` → `400` (gateway DTO `IsIn` rejects before the service); with the flag OFF `{"ghnStatus":"delivering"}` → `403 "GHN demo status endpoint is disabled"`; after the operator set `GHN_DEMO_ENDPOINTS_ENABLED=true` (2026-06-28), valid `{"ghnStatus":"delivering"}` → `201 {orderId:62, previousStatus:"processing", newStatus:"delivering", ghnStatus:"delivering", syncedAt}`, order #62 transitioned `processing→delivering`, a `manual_sync`/`demo_status` `shipping_history` row was written (`success:true`, `payloadSummary.demo:true`, actorId 26), and detail returned `lastGhnStatus:"delivering"` + `return` in `availableActions` — happy path, gate, and DTO validation all confirmed live. FE-facing result recorded in `.agent-local/frontend-handoff-ghn.md` (GHN console channel); closes the demo-status item in `.agent-local/backend-handoff.md`.
- GHN manual sync no longer returns an opaque 502 (2026-06-28): fixed `getOrderDetail` in `apps/orders/src/ghn/ghn.service.ts` — it was the only GHN call (unlike `postOrderMutation` right above it) that did not try/catch its axios request. When GHN replied non-2xx for a stale/unresolvable waybill, or was unreachable, axios threw a **raw AxiosError** (not a NestJS `HttpException`), so `HttpToRpcExceptionFilter` never caught it, it carried no `statusCode` over TCP, and the gateway's `MicroserviceErrorHandler` fell through to its default `BAD_GATEWAY` → `502 {"message":"Internal server error"}` on `POST /api/order/admin/ghn/orders/:id/sync` (FE report on order #62 / `LXD6YV`). Fix wraps the call and maps failures: GHN HTTP rejection (error has a `response`, via new `isGhnHttpRejection` guard) → `NotFoundException` 404 `GHN order <code> not found: <GHN message>`; no HTTP response (timeout/DNS/conn refused) → `ServiceUnavailableException` 503 `GHN detail request failed: <reason>`; HTTP 200 + empty `data` → also 404 (was a misleading `InternalServerErrorException`). Reuses the existing `extractGhnErrorMessage` helper; `response` is now typed `AxiosResponse<GhnDetailResponse>` (new `axios` type import) so the parse block stays type-safe. No migration; success-path contract (`201 {orderId, previousStatus, newStatus, ghnStatus, syncedAt}`) unchanged. Validation: prettier/eslint clean, `tsc --noEmit` 0 errors, `jest apps/orders` 37/37. Runtime self-test (shipmgr_test) on order #62: GHN was reachable this run → `201`, `ghnStatus:"ready_to_pick"`, order stays `processing` — confirms the wiring and that the prior 502 was the now-handled transient/unreachable case (the unreachable→503 / missing→404 branches share the proven `postOrderMutation` error path; GHN could not be forced offline to exercise them live). FE-facing result recorded in `.agent-local/frontend-handoff-ghn.md`; closes the matching item in `.agent-local/backend-handoff.md`.
- Deploy gate G4+G5 — backend now fully DEPLOY-READY (2026-06-28): closed the two remaining (soft) deploy-gate items, so all G1–G5 are done and the backend is deploy-ready. **G4 (`HttpToRpcExceptionFilter` coverage):** the gate text claimed payments/inventory/rewards/product controllers lacked the filter, but a direct audit found they ALREADY carry `@UseFilters(HttpToRpcExceptionFilter)` (committed earlier in 83b567c), and the user service is covered by its own broader global `AllRpcExceptionFilter` (registered in user `main.ts`). The one genuine gap was `apps/notification/src/notification.controller.ts` (3 `@MessagePattern` TCP handlers + 6 `@EventPattern` RMQ handlers, no filter) — added `@UseFilters(HttpToRpcExceptionFilter)` + the import from `@app/common` so its TCP handlers surface proper 4xx instead of 500/502. Gateway is the only HTTP-facing service and uses `HttpExceptionFilter`, correctly excluded. `nest build notification` green; prettier/eslint clean. **G5 (nginx config — verify only):** the gate text said "no `nginx/` dir exists under `api/`" — this was STALE. `nginx/trybuy.conf` (2021 B) + `nginx/trybuy-local.conf` (1375 B) both exist under `api/`. Verified `trybuy.conf` is a valid prod reverse-proxy: HTTP→HTTPS redirect, TLS via letsencrypt, `client_max_body_size 10M`, `location /` → gateway `http://localhost:3000` (with WS upgrade headers + 60s read timeout), `/socket.io/` → `http://127.0.0.1:3010`, `/zalopay/callback` + `/vnpay/callback` → `:3007`; `server_name yourdomain.com` is a placeholder and the header documents the deploy steps (cp to sites-available, symlink, certbot, nginx -t + reload). Operational follow-up at deploy: replace the domain placeholder + run certbot. No code change for G5. **Result: all deploy-gate items G1–G5 are closed → backend is deploy-ready.** Not FE-facing — pure ops/build change. Stale note "payments/inventory/rewards/product chưa có filter" in `conventions.md` corrected to reflect full coverage.
- Deploy gate G1+G2+G3 — production build/run pipeline fixed (2026-06-28): closed the three hard production-readiness blockers so the monorepo can actually be built and supervised in prod. **G1 (build was broken):** `nest-cli.json` had its default project (`sourceRoot`/`root`/`tsConfigPath`) pointing at the long-removed app `apps/ecommerce-nestjs-zzzzz`, so `nest build` with no project failed and there was no `dist/` at all; `package.json` `build` was a bare `nest build` and `start:prod` ran the nonexistent `dist/apps/ecommerce-nestjs-zzzzz/main`. Fix: repointed the nest-cli default project to `gateway`, removed the dead `ecommerce-nestjs-zzzzz` project entry, and set `deleteOutDir:false` so sequential per-service builds accumulate into one `dist/`. `package.json` `build` is now `npm run clean && nest build <svc>` for all 10 real services (gateway, orders, user, product, social, notification, chat, inventory, payments, rewards), with a cross-platform `clean` (`node -e fs.rmSync('dist',…)`); `start:prod` → `pm2 start ecosystem.config.js --env production`; added per-service `start:prod:<svc>` → `node dist/apps/<svc>/main`; dropped the dead `test:e2e` reference to the removed app; renamed the package `ecommerce-nestjs-zzzzz` → `trybuy-api`. Verified: `npm run build` green, all 10 `dist/apps/<svc>/main.js` present. **G2 (ecosystem ran dev mode in prod):** `ecosystem.config.js` previously ran two pm2 apps each calling `npm run start:nodeA|nodeB` → `concurrently` of `nest start <svc> --watch` (webpack/ts watch), so pm2 only supervised the wrapper and a single crashed service was never restarted (the documented nodeB silent-crash footgun). Rewrote it as one pm2 app per microservice running the compiled `node dist/apps/<svc>/main.js` (fork mode, autorestart, `max_memory_restart:'500M'`, `env_production:{NODE_ENV:'production'}`), so pm2 now supervises and restarts each service independently. Per-node start documented in the file header via `--only "gateway,orders,user,product,social,notification,chat"` (Node A) / `--only "inventory,payments,rewards"` (Node B); cwd stays the api root so each service's `dotenv.config({path:"./local/node{A,B}/.env"})` still resolves. **G3 (JWT_SECRET no fail-fast):** gateway booted with an undefined secret and only failed later at auth time. Added a guard in gateway `main.ts` that throws right after `dotenv.config` if `process.env.JWT_SECRET` is absent (before any port bind / `NestFactory.create`), and hardened the `JwtModule` `registerAsync` useFactory in `gateway.module.ts` to throw on an undefined secret instead of silently configuring `secret: undefined`. Gateway is the only JWT signer/verifier (grep-confirmed), so this single guard covers the system. Validation: `npm run build` 0 errors (all 10 services), `tsc --noEmit` 0 errors, prettier/eslint clean on the two touched gateway files. Runtime: `JWT_SECRET="" node dist/apps/gateway/main.js` → exit 1 with "JWT_SECRET is not set — refusing to start the gateway…" (proves fail-fast, thrown before port bind); normal boot with the secret present → `POST /api/user/login` still `201` (JWT signing intact). Remaining deploy-gate items: G4 (add `HttpToRpcExceptionFilter` to payments/inventory/rewards/product controllers — recommended, not a hard blocker) and G5 (verify the nginx conf location at infra/MCR level — verify-only). Not FE-facing — pure ops/build change.
- GHN admin actions — update COD + update receiver (Phase 2 / "Hướng B", B2) (2026-06-28): added the two remaining shop-callable GHN edits so an operator can correct a live waybill's COD amount or receiver name/phone/address before the parcel enters transit, unblocking the two disabled FE buttons in the GHN console. Shop-callability was confirmed by a live GHN dev-API probe (dummy order code → "Đơn hàng không tồn tại", i.e. order-not-found, NOT a permission error — proving both endpoints are shop-callable). New TCP patterns `order.admin_ghn_update_cod` / `order.admin_ghn_update_receiver` (`libs/constant/message-pattern.constant.ts`). `apps/orders/src/ghn/ghn.service.ts` adds `updateOrderCod(code, codAmount)` → GHN `POST /v2/shipping-order/updateCOD` (`{order_code, cod_amount: Math.round(Number(...))}`, DECIMAL→number guard) and `updateOrderReceiver(code, {toName?,toPhone?,toAddress?})` → GHN `POST /v2/shipping-order/update` (sends only the provided `to_name`/`to_phone`/`to_address`), over a shared private `postOrderMutation(action, body)` that throws `InternalServerErrorException` on a non-200 GHN `code` or axios error (message extracted via `extractGhnErrorMessage`). `apps/orders/src/orders.service.ts` adds public `updateAdminGhnCod(orderId, actorId, codAmount)` / `updateAdminGhnReceiver(orderId, actorId, receiver)` over private helpers `loadEditableGhnOrder` (404 if missing, 400 if no `ghnOrderCode`, 400 if the action is not in `getAvailableShippingActions`), `hasText`, and `applyReceiverToShippingAddress` (rewrites only parts [0]=name/[1]=phone/[2]=address of the pipe-delimited `name|phone|addr|ward|district|province`, preserving ward|district|province). Both methods: on GHN reject → record a `success:false` ACTION `shipping_history` row + re-throw, **local order untouched**; on success → persist locally (`order.codAmount` / new `shippingAddress`) + a `success:true` ACTION row + return result. `getAvailableShippingActions` extended: `update_cod` + `update_receiver` offered only for CONFIRMED/PROCESSING with a non-null `ghnOrderCode` (in-transit edits are GHN's call). `updateAdminGhnReceiver` 400s if none of toName/toPhone/toAddress is provided. Wired through `orders.controller.ts` handlers, gateway `order.service.ts` (`timeout(10000)` + `MicroserviceErrorHandler`), new gateway DTOs `apps/gateway/src/order/dto/admin-ghn-update.dto.ts` (`UpdateGhnCodDto` `@IsInt @Min(0)`; `UpdateGhnReceiverDto` optional string fields w/ `@MaxLength`), and gateway routes `POST /api/order/admin/ghn/orders/:id/update-cod` + `:id/update-receiver` (`@CheckPermission("shipping","update:any")`). No DB migration — `ShippingHistoryType.ACTION` already existed. Validation: prettier/eslint clean, `tsc --noEmit` 0 errors, `jest apps/orders/src/orders.service.spec.ts` 37/37 (5 new: COD happy path → GHN updateOrderCod + local update + ACTION row; COD GHN-reject → failed row + no local change; COD past editable window (SHIPPED) → 400; receiver happy path → trims input, rewrites only name/phone parts, updatedFields ["toName","toPhone"]; receiver no fields → 400). Runtime self-test (shipmgr_test, `shipping update:any`) on order 62 (processing, LXD6YV), 7/7: update-cod 0→150000 → 201; update-receiver name+phone → 201 with `shippingAddress` head rewritten + ward/district/province preserved; update-receiver `{}` → 400; update-cod on canceled order 110 → 400; negative codAmount → 400 (DTO); no auth cookie → 401; both ACTION history rows present with `success:true`, actorId 26. FE-facing result recorded in `.agent-local/frontend-handoff-ghn.md` (GHN console channel). This closes "Hướng B" B2 — the only remaining Phase-2 GHN action item; `storing` remains an unused bonus shop-callable action.
- GHN webhook now served at both `/ghn/webhook` and `/api/ghn/webhook` (2026-06-28): removed a footgun where the inbound GHN status callback was reachable ONLY at the un-prefixed `/ghn/webhook` (it was the single route excluded from the global `api` prefix), so a dashboard configured with the natural `/api/ghn/webhook` 404'd silently — i.e. every real GHN status update would have been dropped with no error. Fix serves the same handler at both paths: `apps/gateway/src/ghn/ghn-webhook.controller.ts` changed from `@Controller("ghn")` + `@Post("webhook")` to `@Controller()` + `@Post(["ghn/webhook", "api/ghn/webhook"])`, and `apps/gateway/src/main.ts` adds the second path to the `setGlobalPrefix` exclude list so neither path gets the `api` prefix prepended (the second already carries it literally). Token auth (`x-ghn-webhook-token` / `?token=` vs `GHN_WEBHOOK_SECRET`, timing-safe) and the PascalCase/snake_case body handling are unchanged. No DB/migration; not FE-facing (GHN→backend only, neither frontend calls it). Validation: prettier/eslint clean, `tsc --noEmit -p apps/gateway/tsconfig.app.json` 0 errors. Runtime self-test (gateway hot-reloaded): `POST /api/ghn/webhook` + valid token → 200 (was 404); legacy `POST /ghn/webhook` + valid token → 200 (backward compat preserved); `POST /api/ghn/webhook` + wrong token → 401 (auth intact); used a bogus order code so no real order mutated. Earlier in the same session the full delivery path was also verified by simulated webhook on order 67 (LX4BPD): `delivering` → local DELIVERING, then `delivered` → local COMPLETED, both recorded as `type:"webhook"` history rows — proving the GHN→local status pipeline (`mapGhnStatus`) end-to-end (the GHN dev sandbox itself never advances waybills past `ready_to_pick`, so webhook simulation is the only way to exercise it). Operational follow-up: ensure the GHN dashboard callback URL points at one of the two now-valid paths.
- GHN admin actions — manual cancel + return (Phase 2 / "Hướng B", B1) (2026-06-28): added shop-callable GHN switch-status actions to the shipping console so an operator can cancel or return a live waybill and have the local order reconcile automatically. Scope was set by a live GHN dev-API probe (NOT guessed): GHN `cancel`, `return`, and `storing` are shop-callable, but `delivered`/`deliver`/`delivering` return 400 "Không tìm thấy permission command dựa trên route path" — so **delivery-again is not achievable via GHN's shop API** (GHN drives redelivery internally after a failed attempt) and was dropped from B1; B1 ships cancel + return only. Both GHN endpoints (`POST /v2/switch-status/cancel`, `POST /v2/switch-status/return`) share an identical request/response shape (`{order_codes:[code]}` → `{data:[{order_code, result, message}]}`), so `apps/orders/src/ghn/ghn.service.ts` implements one private `switchOrderStatus(code, "cancel"|"return")` that throws on a falsy per-order `result`; `cancelShippingOrder`/`returnShippingOrder` are thin wrappers (interfaces renamed `GhnCancelResult/Response`→`GhnSwitchStatusResult/Response`). `apps/orders/src/orders.service.ts` adds public `cancelAdminGhnOrder`/`returnAdminGhnOrder` over a shared private `applyAdminGhnAction(orderId, actorId, action)`: it loads the order+items, 404s if missing, 400s if no `ghnOrderCode` or if the action is not in `getAvailableShippingActions(order)`; calls GHN; on GHN rejection it records an ACTION `shipping_history` row with `success:false` and **re-throws, leaving the local order untouched**; on GHN success it flips the order to CANCELED via the concurrency-safe conditional update (`update({id, status: previousStatus}, {status: CANCELED})`, checks `affected===1`), runs the existing `finalizeGhnCancellation` (release reserved stock + emit `ORDER_CANCELED_EVENT`, no cancel pushed back to GHN — GHN is the originator), records a `success:true` ACTION row, and returns an `AdminGhnActionResult`. Both cancel and return resolve locally to CANCELED (GHN return = parcel sent back to the shop). `getAvailableShippingActions` was extended into a real action matrix: `cancel` offered for CONFIRMED/PROCESSING/SHIPPED, `return` for SHIPPED/DELIVERING, both only when a `ghnOrderCode` is present and the order is non-terminal. New TCP patterns `order.admin_ghn_cancel`/`order.admin_ghn_return` (`libs/constant/message-pattern.constant.ts`) wired through `orders.controller.ts` handlers, gateway `order.service.ts` methods (`timeout(10000)` + `MicroserviceErrorHandler`), and gateway routes `POST /api/order/admin/ghn/orders/:id/cancel` + `:id/return` (`@CheckPermission("shipping","update:any")`). No DB migration — `ShippingHistoryType.ACTION` already existed. Validation: prettier/eslint clean, `tsc --noEmit` 0 errors, `jest apps/orders/src/orders.service.spec.ts` 32/32 (6 new: cancel happy path → CANCELED+ACTION row+side-effects; return happy path; GHN-reject → re-throw + no local change + failed ACTION row; non-returnable status → 400; missing order → 404; missing GHN code → 400). Runtime self-test (shipmgr_test, `shipping update:any`): cancel order 110 (processing, LXEWKY) → 201, status processing→canceled, ACTION success row, stock released + event published; detail post-cancel drops cancel/return from availableActions; re-cancel terminal 110 → 400 ("Action \"cancel\" is not allowed ... in status \"canceled\""); read-only `logistics_test` cancel → 403; return order 67 (shipped, LX4BPD) → GHN rejected "Trạng thái đơn hàng không hợp lệ" → 500 with the local order left at `shipped` and a `success:false` ACTION history row recorded (proving the failure path leaves local state intact). FE-facing result recorded in `.agent-local/frontend-handoff-ghn.md` (GHN console channel). Remaining for "Hướng B": B2 (update COD / update receiver) — `storing` is available as a bonus shop-callable action if needed.
- ready-to-ship now resolves the free-text address server-side and gates the PROCESSING transition on a real GHN waybill (2026-06-28): closed the FE-blocking gap where `PATCH /api/order/:id/ready-to-ship` advanced the order to PROCESSING but never created a GHN order, leaving `ghnOrderCode` null (order #112 symptom). Two coordinated changes: (1) **Server-side GHN address resolution** in `apps/orders/src/ghn/ghn.service.ts` — checkout stores the shipping address as free-text pipe-delimited `name|phone|addr|ward|district|province`, but GHN's create-order API requires numeric `to_district_id` + string `to_ward_code`. `buildShippingOrderBody` is now `async` and calls the new `resolveAddressToGhnIds(province, district, ward)`, which walks GHN master-data (`GET /master-data/province|district|ward`, 24h TTL cache) and fuzzy-matches each part via `normalizeAddressPart` (NFD diacritic strip, đ→d, drops VN admin prefixes tp/tinh/quan/huyen/phuong/xa, alnum-only) + `rankMasterDataMatches` (exact-normalized first, then containment ranked by closest length). The resolver walks **all** ranked province→district→ward candidates and falls through stub entries until a full triple resolves — this is required because GHN's dev sandbox has polluted duplicate provinces (e.g. "Hà Nội 02" ProvinceID 2002 with 0 districts shares an alias normalizing to "hanoi" with the real "Hà Nội" 201). Both `createShippingOrder` and `previewShippingFee` now `await buildShippingOrderBody`; the body sends `to_ward_code`/`to_district_id` instead of the old `to_ward_name`/`to_district_name`/`to_province_name`. Unresolvable address → `BadRequestException` (400). (2) **Fail-keep-CONFIRMED transition** in `apps/orders/src/orders.service.ts` `readyToShip` — now loads `relations: ["items"]` (needed by `createShippingOrder`), creates the GHN waybill **before** advancing, persists `ghnOrderCode` via `update`, and only then sets status PROCESSING and saves. The old try/catch that logged-and-silently-advanced is removed: a GHN failure (400 unresolvable / 500 unreachable) now propagates and the order stays at CONFIRMED — never PROCESSING without a waybill. Validation: prettier/eslint clean, `tsc --noEmit` 0 errors, `jest apps/orders/src/orders.service.spec.ts` 26/26 (2 new readyToShip waybill-gating cases: success persists ghnOrderCode + advances; GHN failure rejects + does not save). Live-GHN verification (standalone script against the dev master-data API): order #112's previously-failing address resolves to district 1442 / ward 20101; diacritic/prefix variants resolve; Hà Nội resolves via stub fall-through (201/1485/1A0601); a bogus address fails cleanly → 400. Runtime self-test: confirmed order #110 (techstore_demo) → ready-to-ship → HTTP 200, `ghnOrderCode:"LXEWKY"`, status processing; GHN detail confirms the waybill was created with `to_district_id=1453, to_ward_code=21101` (the resolver's output), proving the ID-based path is live. Note: free-text resolution is best-effort — a garbage/placeholder address (e.g. "District 1 | Ward 1") can still resolve to a wrong-but-valid GHN location rather than failing; collecting GHN IDs at checkout is the durable fix (future work). FE-facing result recorded in `.agent-local/frontend-handoff.md`; closes the ready-to-ship item in `.agent-local/backend-handoff.md`.
- GHN list/detail `lastGhnStatus`/`lastSyncedAt` no longer always null (2026-06-28): fixed a type-coercion bug in `findLatestShippingHistory` (`apps/orders/src/orders.service.ts`). `shipping_history.order_id` is a `bigint` column → mysql2 returns it as a **string** at runtime, so the latest-history `Map` was keyed by `"108"` while the list/detail lookup used `order.id` (int → `108`) — every `map.get()` missed, leaving `lastGhnStatus`/`lastSyncedAt` null on every `GET /api/order/admin/ghn/orders` row and on `GET .../:id` even when a `shipping_history` row existed. Fix coerces `Number(history.orderId)` when keying the map. No migration, no schema change; one fix covers both list and detail. Validation: prettier/eslint clean, `tsc --noEmit` 0 errors, `jest apps/orders/src/orders.service.spec.ts` 24/24. Runtime: order 108 list + detail now both return `lastGhnStatus:"cancel"`, `lastSyncedAt:"2026-06-27T19:33:01.220Z"` (were null). Unblocks the GHN console list "GHN" column + dashboard status distribution. Recorded in `.agent-local/frontend-handoff-ghn.md`; closes the matching item in `.agent-local/backend-handoff.md`.
- GHN cancel/return status now syncs to local CANCELED (2026-06-28): closed a `mapGhnStatus` gap where GHN `cancel` and the whole `return*` family (`waiting_to_return`, `return`, `return_transporting`, `return_sorting`, `returning`, `return_fail`, `returned`) returned `null` → "Unhandled GHN status" → local order stuck while GHN already canceled/returned the parcel. `apps/orders/src/orders.service.ts`: `mapGhnStatus` now maps `cancel` + any status containing `return` → `OrderStatus.CANCELED`; `applyGhnStatus` gained a CANCELED branch that, after winning the concurrency-safe conditional update, runs new `finalizeGhnCancellation(order)` = release reserved stock (idempotent via reservationKey) + emit `ORDER_CANCELED_EVENT` via the extracted `publishOrderCanceledEvent(order)` — but does NOT push cancel back to GHN (GHN is the originator). The forward-only `statusRank` still applies (CANCELED rank 6 > any non-terminal) and terminal orders are still untouched. Affects both GHN-direction paths: inbound webhook `handleGhnWebhook` and manual sync `POST /api/order/admin/ghn/orders/:id/sync`. Validation: prettier/eslint clean, `tsc --noEmit` 0 errors, `jest apps/orders/src/orders.service.spec.ts` 24/24 (2 new cancel/return cases). Runtime: re-sync of already-canceled order 108 now logs "Ignored GHN status \"cancel\" for terminal order" (was "Unhandled GHN status \"cancel\""), confirming the mapping is live and correctly refuses to mutate a terminal order. FE-facing result recorded in `.agent-local/frontend-handoff-ghn.md`.
- `GET /api/user/me` now returns `role` (2026-06-27): fixed `getMe()` in `apps/user/src/user.service.ts` — it previously used a partial column `select` that dropped the `role` relation, so the GHN console could not re-gate a session after reload and force-logged-out valid cookies. A partial `select` that also listed relation columns double-joined the eager `role` on MySQL and 500'd ("Database operation failed" / QueryFailedError); the fix uses a plain `findOne({ where: { id } })` (eager `role` auto-joins, same query shape as `login`) and strips the password via the existing `toSafeUser`. `/me` now returns the same `role` object shape as login (`rol_id`, `rol_name`, `rol_slug`, `rol_status`, `rol_grants`, ...). Validation: prettier/eslint clean, `tsc --noEmit` 0 errors. Runtime self-test: `logistics_test` → `role.rol_name=logistics_operator`; `shipmgr_test` → `role.rol_name=shipping_manager` (grants read:any+update:any). FE-facing result recorded in `.agent-local/frontend-handoff-ghn.md` (GHN console channel); closes the `/me` item in `.agent-local/backend-handoff.md`.
- GHN Web Step 2 — dedicated shipping roles (2026-06-27): closed the `logistics_operator` / `shipping_manager` role-grant gap so the FE no longer hardcodes admin as the shipping role. Added both roles to `apps/user/src/rbac/grants.ts` (`logistics_operator` → `shipping read:any`; `shipping_manager` → `shipping read:any` + `update:any`), extended the `RoleName` enum and the gateway `UserRole` union, and shipped migration `database/add_shipping_roles.sql` (enum extend + `shipping` resource + 2 role rows) applied to Aiven (rol_id 4/5, res_id 7). Authorization is driven by the accesscontrol `ac` from grants.ts; the DB `rol_grants` JSON is documentation-only. Seeded test accounts `logistics_test`/`shipmgr_test` (`Ship@1234`, user ids 25/26). Validation: prettier/eslint clean, `tsc --noEmit` 0 errors. Runtime self-test (4/4): `logistics_operator` GET orders 200 / POST sync 403; `shipping_manager` GET orders 200 / POST sync 400 ("no GHN code" — auth passed); plain `user` GET orders 403.
- GHN Shipping Admin Phase 1.1 hardening: webhook history recording is now best-effort so a `shipping_history` insert failure cannot fail GHN webhook processing after a safe local status transition; the duplicate legacy webhook status block was removed so webhook/manual sync both use the shared GHN status mapper. Verified list/detail/sync/history TCP constants are present and matched. Access remains permission-based through `shipping read:any` and `shipping update:any`; future dedicated `logistics_operator` / `shipping_manager` roles are still a role-grant gap, and no GHN shipper role should be added.
- GHN Shipping Admin Phase 1 backend foundation: added protected Gateway endpoints `GET /api/order/admin/ghn/orders`, `GET /api/order/admin/ghn/orders/:id`, `POST /api/order/admin/ghn/orders/:id/sync`, and `GET /api/order/admin/ghn/orders/:id/history`; added Orders TCP patterns `order.admin_ghn_orders`, `order.admin_ghn_order_detail`, `order.admin_ghn_sync`, `order.admin_ghn_history`; added GHN server-side detail client, safe manual sync through the existing webhook status mapping rules, and active `shipping_history` entity/table + migration (`database/create_shipping_history_table.sql`). Admin `shipping read:any` and `shipping update:any` RBAC grants added. Validation: targeted prettier/eslint passed, `npx.cmd tsc --noEmit` passed, `npx.cmd jest apps/orders/src/orders.service.spec.ts --runInBand` passed (21/21).
- Auth + RBAC: JWT cookie, 64-byte secret, accesscontrol library, RoleAuthGuard, @CheckPermission decorator
- Payments: ZaloPay + VNPay strategy pattern; callback verified; idempotency (UNIQUE order_id); payment-result endpoint; payment_methods table; GET /api/payment/options
- Orders: create/cancel/paginate/admin-list; owner-or-admin guard; PDF invoice; PaginatedResponse
- GHN + COD: full shipping flow — COD order → GHN push → webhook status transitions → payment_completed emit; ZaloPay/VNPay post-payment → GHN; non-fatal failure
- Inventory: atomic reserveStock (UPDATE WHERE); stock sync via RabbitMQ FANOUT (INVENTORY_STOCK_CHANGED_EVENT); findByProductIdOrNull for internal callers
- Product: multi-category ManyToMany; search indexes (7); cache-aside 5s TTL + invalidation; imageUrls JSON column
- Product SKU matrix: product_skus table, variations JSON, upsertSkus transactional; gateway SKU CRUD (GET/POST/PATCH/DELETE /api/products/:id/skus); price/sku nullable on products
- Cart: Cart + CartItem entities (orders service); 5 TCP patterns; gateway fetches authoritative price before forwarding; snapshot columns removed; createOrder() price-injection fix
- User: GET /api/user/me + PATCH /api/user/:id; JWT claim fix (req.user.id); GET_ME + UPDATE_USER patterns
- Social: Post CRUD, Like/Unlike (Redis cache), Comment + Reply tree (materialized-path depth 5), Follow/Feed, isLiked (OptionalJwtAuthGuard)
- Notification: RabbitMQ consumers (payment_completed + order_canceled + comment/reply events) → DB → REST (paginated) + WS push
- Real-time Chat: TCP service (port 3012), WS via gateway:3000/chat, 1-1 + reply, 5-day cleanup cron
- WebSocket: NotificationWsGateway port 3010, JWT auth, rooms user:{userId}, fire-and-forget emit
- Cloudinary: signed upload/delete signature endpoint; image_urls JSON on products + posts; old image_url dropped
- Infrastructure: nginx (Let's Encrypt, WS headers, /zalopay+/vnpay → 3007, /socket.io → 3010), PM2 ecosystem.config.js, trust proxy
- MicroserviceErrorHandler 2-layer: HttpToRpcExceptionFilter on all microservice controllers; @UseFilters applied to payments/inventory/rewards/product
- PaginatedResponse.of() factory in @app/common; PaymentMethod enum in @app/common
- api.md fully updated: 14 controllers, all TCP patterns, RabbitMQ events table
- Product imageUrls fix: gateway CreateProductDto + UpdateProductDto added imageUrls; FE useProductForm + types/index.ts changed image_urls → imageUrls (camelCase)
- RewardPoint entity camelCase: user_id/order_id/created_at → userId/orderId/createdAt with @Column({ name }) aliases; service + controller updated
- Brand/Category approval flow (Phase 1): status ENUM(pending/active/rejected) + submittedBy + reviewNote columns on both tables; POST /api/products/brands|categories now sets status=pending, isActive=false; GET /api/products/brands/pending|categories/pending (admin:read:any); PATCH /api/products/brands/:id/review|categories/:id/review (admin:update:any); brand and category resources added to RBAC grants
- Brand/Category approval flow (Phase 2 — notifications): BRAND_REVIEWED_EVENT + CATEGORY_REVIEWED_EVENT added to EVENT constants; PRODUCT_EXCHANGE ("product.fanout") added to EXCHANGE constants; NOTIFICATION_PRODUCT_SERVICE queue added to QUEUES; ProductModule wired with RmqModule.registerDirectPublisher(); ProductService injects fanoutChannel, emits brand/category reviewed events after review save (fire-and-forget); notification.entity.ts orderId made nullable (number | null); NotificationService.saveNotification() accepts orderId: number | null; notification/main.ts connects to PRODUCT_EXCHANGE; NotificationController adds handleBrandReviewed + handleCategoryReviewed @EventPattern handlers; notification type values: brand_approved, brand_rejected, category_approved, category_rejected; bug fixed: publish payload was missing `pattern` field — NestJS @EventPattern requires `{ pattern, data }` envelope; notifications/order_id column migrated to INT NULL via ALTER TABLE; E2E verified: approve → brand_approved notification, reject-with-note → brand_rejected notification with reason in message
- Brand/Category approval flow (Phase 2b — Admin UI): admin pending brands/categories pages; approve/reject with inline note; RBAC guarded (admin:update:any); nav links in LeftRail
- Brand/Category approval flow (Phase 2c — Product form): brand search-select with Add-as-new inline; category add-new inline; (chờ duyệt) badge on pending items
- CheckoutPage: productMap fetch for authoritative name/image display (api.products.getMultipleWithInventory)
- ProductDetail: variant selection guard on add-to-cart button (disabled until all tiers selected)
- ShopPage: Edit (navigate /sell/:id) and Delete product actions present on ProductRow
- useAuth: migrated to GET /api/user/me via react-query; no localStorage reads for display data
- tsc: zero errors, clean build across all services
- Per-SKU inventory Phase 2+3 complete: orders.service.ts removes skip-logic, adds TCP INVENTORY_RESERVE_STOCK calls per item (SKU-scoped), persists skuId + skuTierIdx (JSON.stringify) on OrderItem; gateway order.service.ts captures tierIdx from SkuPriceResponse and forwards in enriched payload; inventory.controller.ts handleOrderCanceled fixed product_id→productId (camelCase bug); inventory CreateInventoryDto accepts optional productSkuId; orders service fanoutChannel null-guards all 3 publish calls; E2E verified: Test A (simple product order 201 + reservedStock+1 + release on cancel ✅), Test B (SKU order 201 + skuId/skuTierIdx in order_item + reservedStock+2 on correct SKU row ✅), Test C (cancel SKU order → availableStock restored + reservedStock decremented ✅)
- Notification WS moved to gateway: NotificationWsGateway removed from notification service (port 3010 dropped); gateway adds namespace /notifications WS + RMQ consumer (NOTIFICATION_GATEWAY_PUSH_QUEUE bound to notification.push.fanout); notification service emits NOTIFY_USER_PUSH_EVENT via registerDirectPublisher() after saveNotification(); JwtAuthGuard bypasses non-HTTP contexts; tsc clean
- Product search by creator + SKU: added `userId` (filter by creator) and `skuSearch` (LIKE match on product.sku + product_skus.sku via LEFT JOIN) to GetProductsQueryDto in both gateway and product service; `findAllProducts` queryBuilder updated; tsc clean; E2E verified ✅
- Per-SKU inventory Phase 4 complete: SKU_UPSERTED_EVENT ("sku.upserted") added to EVENT constants; INVENTORY_PRODUCT_SERVICE queue added to QUEUES; inventory entity drops unique(product_id), adds @Unique(['productId','productSkuId']) composite constraint (TypeORM synchronize applied); upsertSkus() queries old SKU IDs before delete, emits SKU_UPSERTED_EVENT via PRODUCT_EXCHANGE after transaction commit with payload {productId, skus:[{skuId,sku,stockQuantity}], deletedSkuIds}; inventory/main.ts connects INVENTORY_PRODUCT_SERVICE queue to PRODUCT_EXCHANGE; InventoryService adds createForSku() (idempotent — skips if productId+productSkuId row exists; sku field auto-generated as product-{id}-sku-{skuId}) and softDeleteSkus() (sets isActive=false for given skuIds); InventoryController adds handleSkuUpserted @EventPattern handler; E2E verified: 2 SKUs upserted → 2 inventory rows created ✅, 1 SKU removed → removed SKU row soft-deleted ✅, createForSku skips on same skuId (at-least-once delivery idempotency)

## Recently Completed

- Payment browser-return completion / NEW (2026-06-26): `GET /api/gateway/payment-result` previously treated provider success params as success but only echoed the browser-return query; if the server-to-server callback/IPN was delayed/missing, the payment row stayed `pending` and the order/payment-url state contradicted the success page. The gateway now forwards ZaloPay return params to Payments over `PAYMENT_MESSAGE_PATTERN.COMPLETE_ZALOPAY_RETURN` and VNPay return params over `PAYMENT_MESSAGE_PATTERN.COMPLETE_VNPAY_RETURN`, both with `timeout(10000)` + `MicroserviceErrorHandler`. Payments verifies before writing: ZaloPay `checksum` with `key2` over `appid|apptransid|pmcid|bankcode|amount|discountamount|status`; VNPay via `VNPayStrategy.verifyCallback()` over signed `vnp_*` params. Only verified provider-success returns call `completePayment()`, mark the payment completed, and emit `payment_completed` for Orders. Invalid signatures or failed returns now report `status:"failed"` instead of trusting query params. Duplicate later callbacks remain idempotent and may update `transactionId` if the gateway transaction id arrives later. Regression tests cover verified browser-return completion and invalid-signature no-op for both ZaloPay and VNPay. Runtime verified with live order 112: replaying the signed ZaloPay return URL returned `status:"success"`; authenticated `GET /api/order/112/payment-url` returned `status:"completed"` and order detail no longer stayed pending/unpaid. prettier/eslint/`tsc --noEmit` clean; payments controller/service specs 12/12 pass.

- Payment browser return URL (2026-06-26): new VNPay/ZaloPay payments now return the browser to the frontend result page instead of the backend JSON verification endpoint. `PaymentsService` builds `{FRONTEND_URL}/payment-result?order={orderId}&method={vnpay|zalopay}` for single-order checkout (defaulting to `http://localhost:5173`, first origin when comma-separated) and omits `order` for multi-seller checkout; VNPay passes it as `vnp_ReturnUrl`, ZaloPay as `embed_data.redirecturl`. `GET /api/gateway/payment-result` remains the public JSON verify API for FE fetches. Added focused regression coverage for single-order VNPay and multi-order ZaloPay return URLs. prettier/eslint/`tsc --noEmit` clean; `payments.service.spec.ts` 4/4 pass. Runtime sandbox payment redirect was not re-run because it requires external gateway browser interaction.

- Order snapshot / P2-02 (2026-06-26): historical orders rendered the live product (order item `image` + `skuLabel` were resolved on every read from the product service), so a later product edit or deletion retroactively broke past orders — and every order read paid N product fetches. **Schema:** additive migration `database/add_snapshot_columns_to_order_items.sql` adds two nullable columns `order_items.product_image` (`VARCHAR(2048) NULL`) and `order_items.sku_label` (`VARCHAR(512) NULL`); entity `OrderItem` gains `productImage` / `skuLabel` (`string | null`). Applied to Aiven 2026-06-26 (columns already present with the exact target types — effectively a no-op verify). `product_name` + `sku_id` + `sku_tier_idx` were already snapshot; this completes the snapshot with the image and the human-readable SKU label. **Write path (gateway `OrderService.createOrderInternal`):** the authoritative product fetch already done for price/sellerId now also reads `imageUrls` + `variations` (added to `ProductPriceResponse`), so each enriched item carries `productImage = imageUrls?.[0] ?? null` and, for SKU items, `skuLabel = buildSkuLabel(variations, JSON.stringify(tierIdx))` (null for base-price items) — resolved from the authoritative product, not trusted from the client DTO. Both order-creation paths persist them: single-seller `placeOrder` via the existing `...item` spread (param type widened), multi-seller `placeMultiSellerOrder` via explicit `productImage` / `skuLabel` fields in its `manager.create(OrderItem, ...)` list. **Read path (gateway `decorateItem`):** now prefers the stored snapshot — `image = item.productImage ?? product?.imageUrls?.[0] ?? null`, `skuLabel = item.skuLabel ?? buildSkuLabel(...)` — so the FE-facing `image`/`skuLabel` shape is unchanged. New helper `itemNeedsLiveProduct(item)` filters the `buildProductMap` input across all three read paths (`getOrderById`, `getOrderByUser`, `getSellerOrderDetail`): an item is fetched live only if it lacks an image snapshot or is a SKU selection lacking a label snapshot — so new (snapshot-bearing) orders do **zero** product lookups and a deleted/edited product can no longer break them; legacy pre-migration orders still fall back to the live fetch. prettier/eslint/`tsc --noEmit` clean. Runtime self-test against live nodeA (nodeA `--watch` hot-reloaded), buyer user17 + seller techstore_demo (user23), COD order 108 on product 35 (Samsung Galaxy Watch 6, non-SKU, has `imageUrls`): create → persisted `OrderItem` carries `productImage` = product image + `skuLabel:null`; (1) buyer `GET /api/order/108` → item `image` = snapshot, item filtered out of the product fetch (snapshot present) yet `image` still correct → proves zero-live-lookup resilience; (2) buyer `GET /api/order/user/17` list → same `image`; (3) seller `GET /api/order/seller/108` → `productImage` + `image` = snapshot, `skuLabel:null`. SKU-branch `skuLabel` could not be exercised (no catalog product currently has SKUs) but reuses the already-proven P1-02 `buildSkuLabel`.

- Chat conversation metadata / P1-06 (2026-06-26): `GET /chat/conversations` returned bare `Conversation` rows (`id/user1Id/user2Id/createdAt`), so the FE messages list had no last-message preview and no unread badge (it fell back to `@username` + `createdAt`). **Schema:** additive migration `database/add_read_tracking_to_conversations.sql` adds two nullable per-side read markers `conversations.user1_last_read_at` / `user2_last_read_at` (`DATETIME NULL`; NULL = never opened → all inbound counts as unread); entity `Conversation` gains `user1LastReadAt` / `user2LastReadAt` (`Date | null`). Applied to Aiven 2026-06-26. **Read path (`ChatService.getConversations` → new `ConversationWithMeta[]`):** after loading the viewer's conversations it runs two bounded batch queries (no N+1) over the conversation-id set — (a) latest message per conversation via `INNER JOIN (SELECT conversation_id, MAX(id) ...)` keyed on `MAX(id)` (auto-increment bigint, avoids same-second `created_at` ties) → `lastMessage {id,content,senderId,createdAt} | null`, and (b) a single grouped `COUNT(*)` of inbound unread (`sender_id != viewer` AND newer than the viewer's side-correct read marker, the user1/user2 branch chosen in SQL) → `unreadCount`. Results are sorted active-first (`lastMessage.createdAt ?? conversation.createdAt` desc). **Mark-read:** new `CHAT_MESSAGE_PATTERN.CHAT_MARK_READ` → `ChatService.markRead(userId, conversationId)` (membership check → `ForbiddenException`; stamps the viewer's side `lastReadAt = now()`, returns `null` per the void-handler rule) exposed as `POST /api/chat/conversations/:id/read` (JwtAuthGuard) with a gateway passthrough (`timeout(10000)` + `MicroserviceErrorHandler`). prettier/eslint/`tsc --noEmit` clean. Runtime self-test 4/4 against live nodeA (chat `--watch` hot-reloaded), conv 17↔18 with 2 WS-sent messages from user18: (1) user17 `GET /chat/conversations` → conv1 `unreadCount:2` + `lastMessage` = latest, active-first ordering, other (message-less) conversations `lastMessage:null,unreadCount:0`; (2) `POST .../1/read` → 201, re-GET → `user1LastReadAt` set + `unreadCount:0`, `lastMessage` retained; (3) per-side independence — user18 (sender) sees `unreadCount:0` and its own `user2LastReadAt` still null while user17's marker is set; (4) `POST .../2/read` on a non-member conversation → 403 "Access denied".

- nodeB idle-crash root-cause fix + gateway inventory resilience (2026-06-26): nodeB services (inventory/payments/rewards) were silently dying after an idle period (machine sleep / local RabbitMQ restart), which surfaced to the FE as `POST /products/with-inventory/multiple` and `GET /products/with-inventory/all` returning **500 AggregateError** (ECONNREFUSED to inventory:3002) and `GET /products/shop/stats` 502. **Root cause:** `RmqModule.registerDirectPublisher()` (`libs/common/src/rmq/rmq.module.ts`) opened a raw `amqplib` connection + channel but attached **no `'error'`/`'close'` listeners** — an amqplib connection is an EventEmitter, so an unhandled `'error'` on an idle drop is thrown as an **uncaught exception and crashes the whole service**. The `nest start <svc> --watch` wrapper kept running (and does NOT auto-restart a runtime crash), which masked the dead app process. **Fix (shared lib → all 6 publisher services: inventory/payments/orders/product/social/notification):** the factory now (a) attaches `error`/`close` handlers on both connection and channel (log, never throw), (b) appends `?heartbeat=30` to the URI so dead sockets are detected, (c) connects in the **background** (sync factory returning immediately — a missing/slow broker never blocks or crashes bootstrap; TCP handlers always come up), and (d) **auto-reconnects** every 5s after a drop, exposed through a stable self-healing `Proxy<Channel>` so the 6 injected consumers keep a single reference and `publish()` safely no-ops (returns `false`) while the channel is briefly down. **Secondary fix (gateway `getProductsWithInventory` in `apps/gateway/src/product/product.service.ts`):** the `/with-inventory/multiple` path was the only inventory-aggregation method missing graceful degradation — no `timeout(10000)` on the inventory `.send()` and a bare `throw error` re-raised the raw `AggregateError` as a 500. Now mirrors `getProductWithInventoryById`: `.pipe(timeout(10000))` + `.catch()` → degrade to `inventory: null` when inventory is unreachable, and each per-product fetch is `.catch(() => null)` (the existing `filter(p !== null)` already expected nulls). eslint/prettier/`tsc --noEmit` clean. Runtime-verified after the fix: inventory recompiled and came back on 3002; `POST /api/products/with-inventory/multiple {productIds:[47]}` → 201 with populated inventory (`available=100,total=100,low=false`; previously the 500), and `GET /api/products/shop/stats` (shop `techstore_demo`) → 200 `{productCount:12,totalStock:961,lowStockCount:0}` — which also closes the P2-05 shop-stats runtime-pending item.

- Pagination / stat endpoints / P2-05 (2026-06-26): three additive, independent endpoints so the FE can drop its high-`limit` workarounds without hiding data; no existing route/test changed. **(a) Admin users pagination:** new `USER_MESSAGE_PATTERN.GET_USERS_PAGINATED` (`{ cmd }` wrapper, like the other user patterns) → `UserService.getUsersPaginated(page, limit)` uses `findAndCount` with the same `select`/`toSafeUser` projection as `getAllUsers` (password never leaves the service), `order: { createdAt: 'DESC' }`, and returns `PaginatedResponse.of(...)`. Exposed as `GET /api/user?page=&limit=` (`@Roles("admin")`, `ListUsersQueryDto` with `@Type(() => Number)` + `@IsInt @Min(1)` page / `@Min(1) @Max(100)` limit, defaults 1/20). Root `@Get()` declared after `@Get("all")` and before `@Get("me")`/`@Get(":id")` — no route collision. Legacy `GET /api/user/all` (unpaginated) left intact. **(b) Shop stats:** new gateway `GET /api/products/shop/stats` (authenticated, seller-scoped via `req.user.id`) → `ProductService.getShopStats(sellerId)` does a bounded 2-call aggregation (no N+1): `GET_PRODUCT_IDS_BY_SELLER` then batch `INVENTORY_GET_BY_PRODUCT_IDS`, returning `{ productCount, totalStock, lowStockCount }` where `totalStock = Σ availableStock` and `lowStockCount` = distinct products with any `availableStock <= minimumStock` row. **(c) Notifications unread badge:** new `NOTIFICATION_MESSAGE_PATTERN.GET_UNREAD_COUNT` (plain string) → `NotificationService.countUnread(userId)` (`repository.count({ where: { userId, isRead: false } })`) → gateway `GET /api/notifications/unread-count` returning `{ unreadCount }`. prettier/eslint/`tsc --noEmit` clean; user.service.spec 2/2 green. Runtime self-test against live nodeA: admin pagination PASS (`GET /api/user?page=1&limit=3` → 200 `{total:10,page:1,limit:3,totalPages:4,hasNext:true}`, DESC order, no password), non-admin guard PASS (shop account → 403 "Insufficient role"), unread-count PASS (`{unreadCount:22}`, cross-checked against the notification list). Shop stats is implemented and type-clean but **runtime-pending**: nodeB/inventory (3002) was down this session, so the inventory aggregation could not be exercised (the pre-existing `GET /api/products/with-inventory/all` failed identically) — re-verify once nodeB is up.

- Multi-category hydrate / P1-04 (2026-06-25): the product service already returns the full hydrated `categories[]` (eager `@ManyToMany` via `product_categories`) on every read path, but only the gateway `getProductById` derived a flat `categoryIds: number[]` — so the FE shop table's multi-category editor saw an inconsistent shape across list vs detail. **No schema change** (the join table and eager relation already exist); this was purely a gateway response-shape normalization. Added two private gateway helpers in `apps/gateway/src/product/product.service.ts`: `attachCategoryIds(product)` (derives `categoryIds` from `categories[]`, coercing the string ids the MySQL driver returns via `Number()`) and `withCategoryIds(response)` (applies it across a bare product, an array, or a paginated `{ data | items: [] }` envelope). Wired into **every** product read path: `getAllProducts` + `getAllProductsWithInventory` (via the shared `fetchProductsPage` chokepoint), `getProductById` (replaced the old inline block), `getProductBySku`, `getProductsByCategory`, `getProductsByBrand`, `searchProducts`, and the single `getProductWithInventory`. Every product object now carries both the full `categories[]` and a flat `categoryIds: number[]`. prettier/eslint/`tsc --noEmit` (apps/gateway) clean. Runtime self-test passed 5/5 against the live nodeA stack (`--watch` hot-reloaded): list, by-id, by-sku, by-category, and search all return `categoryIds`; multi-category products correctly return multiple ids (e.g. `[20,21]`, `[15,21]`) alongside the matching full category objects.

- Social↔commerce relation / P1-03 (2026-06-25): a social post could only carry `content/imageUrls/videoUrl`, there was no way to edit a post, and the FE post overflow-menu had no report target. **(a) Product attach:** `posts.product_id INT NULL` column added (entity `Post.productId: number | null`, mapped `@Column({ name: 'product_id' })`); the create-post path (gateway `CreatePostDto.productId` optional `@IsInt @Min(1)` → gateway service → `CREATE_POST` payload → service persist) now stores it and every post read returns `productId`. Deliberately no server-side product hydration — the FE hydrates via its existing `useProductsByIds` (same pattern used for order items), so the post response stays cheap. **(b) Edit:** new TCP pattern `SOCIAL_MESSAGE_PATTERN.UPDATE_POST` → `SocialService.updatePost` (owner check → `ForbiddenException`; partial — only fields present in the payload overwrite, `undefined` = leave as-is) exposed as `PATCH /api/social/posts/:id` (JwtAuthGuard, `UpdatePostDto extends PartialType(CreatePostDto)`). **(c) Report:** new `post_reports` table + `PostReport` entity (`@Unique(['postId','reporterId'])` so one report per user per post), TCP pattern `REPORT_POST` → `SocialService.reportPost` (post-exists check, self-report → `BadRequestException`, dup → `ConflictException`) exposed as `POST /api/social/posts/:id/report` (JwtAuthGuard, `RateLimit 20`, `ReportPostDto.reason` ≤500). Two additive migrations `database/add_product_id_to_posts.sql` + `database/create_post_reports_table.sql`; social module registers `PostReport`. prettier/eslint/`tsc --noEmit` clean. Both migrations applied to Aiven and runtime self-test passed 8/8 against the live nodeA stack (`--watch` hot-reloaded the edits): create-with-productId 201, GET returns productId, owner PATCH (full + partial leave-as-is) 200, non-owner PATCH 403, report 201, duplicate report 409, self-report 400, delete 200.

- Order item enrichment + per-status counts / P1-02 (2026-06-25): buyer-facing order responses exposed only raw `OrderItem` fields, and order-tab counts were computed by the FE over the loaded page only. **(a) Item enrichment:** extracted the seller-detail enrichment into two reusable gateway helpers — `buildProductMap(productIds)` (deduped, parallel `PRODUCT_FIND_BY_ID` fetch, per-product failure logged-and-skipped so one missing product never fails the response) and `decorateItem(item, productMap)` (adds `image` = `imageUrls[0]` and `skuLabel` via the existing `buildSkuLabel`). Applied to `GET /order/:id` and `GET /order/user/:id` (list path batches one product fetch across every item on the page, not per order) in addition to the existing `GET /order/seller/:id`. So all three order read paths now return `skuId` + `skuLabel` + `image` per item. **(b) Per-status counts:** new orders TCP pattern `GET_ORDER_STATUS_COUNTS` → `OrdersService.getStatusCountsByUser(userId)` runs a single `GROUP BY order.status` query and returns every `OrderStatus` key zero-filled plus an `all` total; exposed as `GET /api/order/user/:id/status-counts` (three-segment route declared before `user/:id`, JwtAuthGuard, owner-or-admin check). Counts span the buyer's full order history, not the loaded page. Snapshot of name/image/SKU at purchase time is still deferred to P2-02 (today's `image` is resolved live from the product service). gateway+orders Jest suites 29/29; prettier/eslint/`tsc --noEmit` clean. Runtime endpoint self-test deferred — requires the full nodeA+nodeB stack against Aiven cloud MySQL.

- Seller order lifecycle / P1-01 (2026-06-25): sellers were blind to order detail (the buyer-facing `GET /order/:id` 403s anyone who is not the order owner or admin) and could only push `pending→confirm` and `confirmed→ready-to-ship`; everything after `processing` was webhook-only. Added two orders TCP patterns — `GET_SELLER_ORDER_DETAIL` and `ADVANCE_ORDER_STATUS`. **(a) Detail:** `OrdersService.getSellerOrderDetail(orderId, sellerId, isAdmin)` loads the order with `items`, admin bypasses ownership, otherwise verifies the seller owns ≥1 line item (reusing `getSellerProductIds` + `verifySellerOwnsOrder`) → `ForbiddenException`. The gateway `getSellerOrderDetail` enriches each item with the product image (`imageUrls[0]`) and a human-readable `skuLabel` built from `product.variations` + the per-item `skuTierIdx` (new `buildSkuLabel` helper: JSON-parses the tier-index string and maps each option index to `"VariationName: Option"`). Exposed as `GET /api/order/seller/:id` (two-segment route, declared before `:id` so no shadowing). **(b) Transitions:** `OrdersService.advanceOrderStatus` uses a static `SELLER_FORWARD_TRANSITIONS` map (processing→shipped→delivering→completed) — single-step-forward only — and applies the move with the **same guarded conditional UPDATE** the GHN webhook uses (`update({id,status:current},{status:target})`; `affected!==1` → `ConflictException`), so a seller action and a delivery webhook racing the same transition can't double-run completion. The COMPLETED side effects (consume reserved stock + emit `payment_completed` for COD) were extracted out of `handleGhnWebhook` into a shared `private finalizeOrderCompletion(order)`, now called by both the webhook and the seller path → completion runs at most once per order. Gateway endpoints `PATCH /api/order/:id/ship|deliver|complete` (status passed as a string literal to avoid importing the orders enum across apps). Added 4 orders regression tests (advance ok / non-sequential reject / conflict on lost race / COD finalize once) — orders suite 21/21; prettier/eslint/`tsc --noEmit` (apps/orders + apps/gateway) clean. Runtime endpoint self-test deferred — requires the full nodeA+nodeB stack against Aiven cloud MySQL.

- SKU diff + reference protection on product edit / P0-05 (2026-06-25): `ProductService.upsertSkus` was a destructive delete-all-then-recreate inside one transaction, which churned every SKU `id` on each product edit and orphaned any `order_item`/`cart_item` that referenced the old ids. Rewrote it as a create/update/delete **diff keyed on the variation combination (`tierIdx`)** — the natural key, since the SKU DTO carries no `id`. A new `canonicalTierKey()` normalizes both the raw VARCHAR string (`"[0,1]"`) and the `@AfterLoad`-parsed `number[]` to the same spaceless JSON so they compare reliably. Matched SKUs are **updated in place** (id preserved, so references survive); SKUs absent from the incoming list are "removed candidates". A new orders TCP pattern `ORDER_MESSAGE_PATTERN.GET_REFERENCED_SKU_IDS` → `OrdersService.findReferencedSkuIds` queries both `order_items` and `cart_items` for matching `sku_id`; the product service calls it **outside the DB transaction** (no lock held across the TCP round-trip) and on any failure treats every candidate as referenced (fail-safe — a transient orders outage can never silently delete data). Referenced removed SKUs are **soft-deactivated** (`isActive=false`, row kept); unreferenced ones are hard-deleted. `SKU_UPSERTED_EVENT` still fires with `deletedSkuIds` now meaning only the hard-deleted set, so inventory only drops stock for SKUs that truly went away. Wired `ORDERS_SERVICE` TCP client into `ProductModule` + `ProductService` (with `onModuleInit` connect). Added a new product Jest spec (update-in-place / hard-delete-unreferenced / soft-deactivate-referenced) — 3/3; gateway+orders+product suites 28/28; prettier/eslint/`tsc --noEmit` (apps/product + apps/orders) clean. Runtime endpoint self-test deferred — requires the full nodeA+nodeB stack against Aiven cloud MySQL.

- Create-order idempotency / P0-04 (2026-06-25): the gateway `POST /api/order` endpoint now accepts an optional `Idempotency-Key` header. `OrderService.createOrder` wraps the existing logic (renamed to `createOrderInternal`) in a Redis single-flight guard via the shared `CachedService`: a new `setNx()` helper (`SET key val EX ttl NX`) atomically claims `idem:order:{userId}:{key}` with a 60s in-flight lock; a concurrent double-submit while the first request is in flight gets `409 ConflictException`, and a retry after completion replays the cached response (orders + paymentUrl, 24h TTL) without creating a duplicate order or re-initiating payment. The cached response covers payment-initiation too, since the single-seller/multi-seller paths initiate payment within the same call. Failures release the lock (`del`) so a legitimately failed request can be retried, and any Redis error degrades gracefully to a plain (non-idempotent) create so the cache can never block a real order. Requests without the header behave exactly as before. `OrderModule` already imported `CachedModule`. Added 3 regression tests (replay, 409 in-flight, lock release on failure); gateway Jest suite 8/8 passes; prettier/eslint/`tsc --noEmit` (apps/gateway + libs/cached) clean. Runtime endpoint self-test deferred — requires the full nodeA+nodeB stack against Aiven cloud MySQL.

- Atomic product+inventory create & orphan cleanup / P0-03 (2026-06-25): closed three defects in the cross-DB (MySQL product ↔ PostgreSQL inventory) create/delete path. (1) Stale 409: `InventoryService.create` now looks up an existing `productId`+`productSkuId=NULL` row; if it is inactive (soft-deleted) it is hard-deleted before re-create instead of throwing "already exists" — only an active row still 409s. (2) Orphan cleanup on delete: gateway `deleteProduct` now calls a new `INVENTORY_REMOVE_BY_PRODUCT` TCP pattern → `InventoryService.removeByProductId` (hard delete of all rows for the product) after the product delete, so a deleted product's inventory never lingers to be re-attached to a reused ID. (3) Saga compensation on create: gateway `createProduct` now rolls back the product (`compensateProductCreate`) if the follow-up inventory create fails, instead of swallowing the error and leaving a product with no/zero stock. Added `INVENTORY_UPDATE`/`INVENTORY_REMOVE_BY_PRODUCT` constants and the controller handler. No FK/cascade possible across the two databases, so cleanup is the compensation mechanism. prettier/eslint/`tsc --noEmit` (apps/inventory + apps/gateway) clean; inventory spec passes. Runtime self-test deferred — requires the full stack.

- Stale-reservation sweeper (2026-06-25): orders that can never reach the GHN "delivered" webhook (online-payment orders abandoned before payment, COD orders a seller never confirmed, or orders whose GHN creation failed so `ghn_order_code` stays null) previously held reserved stock forever. `OrdersService` now runs an hourly `@Cron(EVERY_HOUR)` `sweepStaleReservations()` that selects orders in PENDING/CONFIRMED/PROCESSING with `ghn_order_code IS NULL` and `created_at` older than `ORDER_STALE_RESERVATION_TTL_HOURS` (default 24h) and cancels each one. The cancel side effects (`updateOrderStatus(CANCELED)` → idempotent `releaseReservedItems` → GHN cancel → `ORDER_CANCELED_EVENT`) were extracted from `cancelOrder()` into a shared private `finalizeCancellation()`, so a swept order releases its reservation through the exact same idempotent path as a user-initiated cancel. Orders already handed to GHN (code set) are never swept — their terminal state stays driven by the delivery webhook. A reentrancy guard skips overlapping ticks, and per-order try/catch isolates a single failure so the rest of the batch still sweeps. `ScheduleModule.forRoot()` added to `OrdersModule`. Added 5 focused regression tests (cancels+releases, query shape, no-op on empty, batch isolation, overlap skip); full orders Jest suite 17/17 passes; prettier/eslint/`tsc --noEmit` (apps/orders) clean. Optional env override `ORDER_STALE_RESERVATION_TTL_HOURS` documented in snapshot ops reference (the `local/nodeA/.env` file is gitignored/permission-denied, so the in-code default of 24 applies unless set there manually).

- SKU tierIdx integrity guard (2026-06-24): `ProductService.upsertSkus` now loads the product's `variations` inside the transaction and calls a new private `validateSkuTiers()` that rejects any SKU whose parsed `tierIdx` length ≠ variation count, or whose any index falls outside its variation's option range — throwing `BadRequestException` before the existing rows are deleted, so a bad payload can never wipe good data. This closes the previously-tracked corrupt-SKU known issue (product #7 `HOODIE-UNISEX-001`/`HOODIE-WHT` with `tierIdx:[0,1]` against a single 3-option variation). The data side was already clean: a user-approved Aiven inspection found product #7 no longer exists (deleted, SKU removed via CASCADE) and the only remaining SKU rows (product #2 `[0]`/`[1]`) are well-formed — so no destructive migration was needed. Self-tested as admin against product #2: `[0,1]`→400, `[5]`→400, and the two good SKUs verified untouched afterward. prettier/eslint/`tsc --noEmit` (apps/product) all pass.

- Distributed order creation compensation: single-seller online-payment orders now fail fast and compensate when `order_created` cannot be published to RabbitMQ, marking the saved order canceled and releasing reserved stock via Inventory TCP. Multi-seller checkout now cancels all child orders if synchronous payment initialization fails after order creation. `cancelOrder()` also releases reservation stock synchronously before publishing the existing cancel event, keeping RabbitMQ release idempotent as a follow-up consumer path. Added focused orders/gateway regression tests; targeted Jest, ESLint, and `npx tsc --noEmit` pass.

- Inventory reservation idempotency: Orders now generates a UUID reservation key before stock reservation, persists it on each order, and forwards it through compensation, cancellation, and delivery-consume flows. Inventory stores each reservation in `inventory_reservations` and atomically transitions it from `reserved` to `released` or `consumed`; duplicate reserve/release/consume delivery returns the prior result without mutating stock again, while conflicting terminal transitions fail. The cancellation consumer now nacks/requeues a failed item transition instead of acknowledging partial failure. Added MySQL/PostgreSQL migrations and a direct duplicate-reserve/release regression test. Orders/inventory builds, `tsc --noEmit`, full Jest (12/12 suites, 43/43 tests), and full ESLint pass with only 7 pre-existing e2e typing warnings.

- Successful one-click e-commerce Postman automation: created `TryBuy Full E-commerce Success E2E` (`22291119-41ddef8a-aa24-4510-ab29-3c4a533f200f`) with 33 requests. The existing personal environment now stores `GHN_WEBHOOK_SECRET` as a secret variable. The flow creates the image/product/inventory fixture, performs buyer cart + COD checkout, switches to the shop for confirm + ready-to-ship, submits an authenticated GHN `delivered` webhook, verifies `completed` plus reserved-stock consumption, creates/lists a verified-purchase review, and cleans review/cart/inventory/product/media while retaining the completed order as audit history. Runtime produced completed COD order 106 with GHN code `LXYG4W`; generated product 23 and all removable fixtures were cleaned. The MCP result callback timed out after the flow exceeded its callback window, but the runner used `stopOnFailure=true` and reached cleanup, and final API state confirms successful completion.

- Full one-click e-commerce Postman automation: created personal environment `TryBuy Local Full E2E` (`22291119-269f6e3d-0995-4c33-8691-4bbd7ad12670`) with approved test-account credentials stored as secret variables, and collection `TryBuy Full E-commerce E2E` (`22291119-c0dbed80-73a8-435b-9559-bdc155596b4e`). The deterministic COD flow covers public preflight, shop login, active-category selection, signed Cloudinary image upload, product creation, deterministic inventory setup, buyer login, cart, GHN fee preview, checkout, order lookup, stock reservation, cancellation, stock release, and cleanup of cart/inventory/product/media. Inventory setup now branches safely: a successful lookup skips creation, while a 404 creates the missing row before applying test stock, preventing `/inventory/undefined`. Latest Postman MCP run executed 24 requests and passed 39/39 assertions with 0 failures in 51.78s; the canceled order remains as the audit record.

- Product image upload automation: fixed `UploadController` to use the normalized JWT `req.user.id` instead of the missing `req.user.userId`, preventing generated Cloudinary public IDs from being prefixed with `undefined`; added a focused controller regression test and documented the signed direct-upload contract. Created remote Postman collection `TryBuy Product Image Upload Flow` (`22291119-f0707c0e-1700-4ef4-84f2-ad96acaaac70`) with login, active-category selection, signed Cloudinary upload, product creation, and persisted-image verification. Runtime E2E with shop user 20 uploaded a generated PNG, created product 17, produced public ID prefix `20_`, and verified the exact `secure_url` round-trip; targeted Jest, ESLint, and `tsc --noEmit` pass.

- Gateway dead endpoint cleanup: removed the unused legacy `POST /api/gateway` order alias and fake `GET /api/gateway/health` endpoint. `GET /api/gateway/payment-result` remains because ZaloPay/VNPay redirect configuration and `PaymentResultPage` consume it. Removed the now-empty `GatewayService`; retained `OrderModule` ownership of its four TCP clients to avoid the former gateway/order module cycle.

- RabbitMQ acknowledgement + public-route auth alignment: Payments and Rewards `order_created` consumers now ack every successful/intentional-skip path and nack+requeue processing failures; regression tests cover success and failure behavior. Product search/category/brand/SKU/by-id/with-inventory/stock-check routes and Inventory read/check-stock routes now match their documented public auth zone. Runtime requests without cookies reached all 15 routes without a 401 response. Gateway/payments/rewards builds, `tsc --noEmit`, full Jest (10/10 suites, 41/41 tests), and full ESLint pass with only 7 pre-existing e2e typing warnings. Root `npm run build` remains misconfigured to target the nonexistent `apps/ecommerce-nestjs-zzzzz/tsconfig.app.json`.

- Payment completion idempotency: ZaloPay/VNPay completion now atomically transitions a payment from pending to completed, so duplicate or concurrent callbacks return success without re-emitting `payment_completed`. Orders atomically claims pending/confirmed orders as processing before calling GHN, so duplicate/redelivered events cannot create a second shipping order. Added callback and racing-event regression tests. Runtime sent the same valid VNPay callback twice for multi-payment 511; child orders 99/100 each received exactly one GHN code. Test orders were canceled and stock restored. Gateway/orders/payments builds, `tsc --noEmit`, full Jest (10/10 suites, 38/38 tests), and full ESLint pass with only 7 pre-existing e2e typing warnings.

- Multi-seller payment deduplication: every multi-seller `order_created` event now carries `isMultiSellerCheckout=true`; Payments skips individual child-order payment creation and leaves payment initialization to the existing synchronous multi-order flow. Added producer/consumer contract regression tests. Runtime checkout created orders 99/100 and exactly one payment row (`id=511`, `order_id=NULL`, `order_ids=[99,100]`); both payment-url endpoints returned the same multi-order URL.

- Reserve stock before order commit: single- and multi-seller checkout now atomically reserve Inventory stock before opening the Orders DB transaction; partial reservation failures and DB transaction failures compensate reserved items through `RELEASE_STOCK`, while reserve=false prevents order persistence. Added 5 fail-path/order-of-operations regression tests. Runtime verified non-COD order 98 for product 5 changed stock exactly once from available/reserved 29/0 to 28/1, then cancellation restored 29/0. Gateway/orders builds, `tsc --noEmit`, full Jest (9/9 suites, 32/32 tests), and full ESLint pass with only 7 pre-existing e2e typing warnings.

- Inventory write authorization: create/update/delete inventory now require product owner or admin; SKU-scoped inventory creation verifies `productSkuId` belongs to the product. HTTP reserve/release are admin-only because checkout/cancel use internal TCP/RabbitMQ flows. Check/reserve/release DTOs now accept and forward optional `skuId`. Added 6 regression tests. Runtime verified foreign reserve/update/create return 403, owner update returns 200, and admin reserve remains available; full Jest suite passes 9/9 suites, 27/27 tests; gateway build, tsc, and ESLint pass with only 7 pre-existing e2e typing warnings.

- Product ownership enforcement: update/delete product and create/update/delete SKU now require the authenticated product owner or admin at the gateway; SKU update/delete also verifies the SKU belongs to the product ID in the route, preventing cross-product SKU substitution. Added 6 regression tests. Runtime verified foreign product PATCH, SKU create, and SKU PATCH return 403 while owner/admin product PATCH return 200; full Jest suite passes 8/8 suites, 21/21 tests; gateway build, tsc, and ESLint pass with only 7 pre-existing e2e typing warnings.

- Order IDOR fixes: `GET /api/order/user/:id` and `GET /api/order/:id/payment-url` now enforce owner-or-admin access at the gateway before returning order/payment data; payment URL lookup verifies order ownership before calling Payments. Added 4 regression tests and the missing Jest `libs/*` alias; corrected Payments/Rewards controller test scaffolds to mock their services. Runtime verified both foreign-user paths return 403 and admin paths return 200; full Jest suite passes 7/7 suites, 15/15 tests.

- Removed obsolete `scripts/health-check-nodeA.sh` and `scripts/health-check-nodeB.sh`: they sent HTTP `/health` requests to Nest TCP ports with stale service mappings, causing `InvalidTcpDataReceptionException`, and were not used by application startup. Deployment validation now directs operators to `pm2 list` and no longer requires curl solely for these scripts.

- Critical user API security fixes: `RoleAuthGuard` now enforces `@Roles()` metadata, so `GET /api/user/all` rejects non-admin users; user list queries explicitly exclude password and defensively sanitize returned entities; registration responses also remove the password hash before crossing TCP. Added 4 focused regression tests. Runtime verified: ordinary user 403, admin 200, no password field; gateway/user builds, targeted ESLint, and `tsc --noEmit` pass.

- GHN webhook security + transition integrity: gateway now requires `GHN_WEBHOOK_SECRET` through `x-ghn-webhook-token` or `?token=` and compares it in constant time; missing/invalid authentication returns 401; orders applies monotonic terminal-state guards and an atomic conditional update so canceled/completed orders, stale callbacks, duplicate callbacks, and concurrent delivered callbacks cannot regress state or repeat stock/payment side effects; GHN TCP handler now returns `null` instead of `void`; Postman collection inherits secret auth; focused Jest regression suite passes 4/4; runtime smoke test passed (401 without token, 200 with token); tsc + targeted eslint clean

- Postman MCP workflow documented in `AGENTS.md`: mandatory `postman://instructions` read, reuse-before-create, collection UID/environment rules, safe external-state handling, localhost runner fallback, test-account/secret handling, and structured run-result reporting; MCP authentication/workspace discovery verified; remote personal collection `TryBuy MCP Smoke Tests` (`22291119-00d98cb6-2558-422a-a1bf-1192bba1f005`) created with 3 read-only public requests; `runCollection` passed 3/3 assertions (gateway health, payment options, products page), 0 failed, 6.86s

- AI documentation directory renamed from `docs/` to `ai-docs/` so repository documentation is clearly distinguished from agent context; all Codex/Claude entry points, skills, permissions, and internal references now use the new path

- Agent context consolidation: moved duplicated `.codex`/`.claude` context into `ai-docs/agent-context/`, moved the shared handoff to `ai-docs/agent-handoff/snapshot.md`, and unified local test accounts at `.agent-local/test-accounts.md`; `AGENTS.md` and `.claude/CLAUDE.md` remain tool-specific entry points and now reference the same source of truth

- GHN webhook PascalCase fix + Postman collection: ghn-webhook.controller.ts now reads BOTH field shapes — PascalCase (OrderCode/Status, what real GHN sends) and snake_case (order_code/status, manual tests), PascalCase preferred; root cause: controller only read snake_case → real GHN callbacks parsed as undefined → "Order not found" log + 200, order status never advanced (silent fail, no GHN retry); guard added: missing both shapes → warn log + 200 no-op; postman/ghn-webhook.postman_collection.json created (7 requests: picking/picked/delivering/delivered lifecycle, unhandled status, nonexistent code, PascalCase shape; baseUrl + ghnOrderCode collection vars; built-in 200+success assertions) so user can self-test; tsc + eslint clean; E2E: COD order 95 (product 5, GHN code LXTCRP) → PascalCase "picking" → shipped ✅, snake_case "delivering" → delivering ✅, missing-fields body → 200 no-op ✅, PascalCase "delivered" → completed + reserved 1→0 consumed ✅

- Payment gateway selected by user's paymentMethod (env decoupled): PaymentGatewayFactory.getStrategy(method: PaymentMethod) now takes the method as a parameter — PAYMENT_GATEWAY env no longer read anywhere (zalopay → ZaloPayService, vnpay → VNPayStrategy, else BadRequestException); processPayment + processMultiOrderPayment accept paymentMethod (enum aliased as PaymentMethodEnum in payments.service.ts to avoid collision with PaymentMethod entity); gateway order.service.ts forwards paymentMethod in INITIATE_MULTI_ORDER_PAYMENT payload; BUG FIXED: payments handleOrderCreated read order.payment_method (snake_case) but orders emits paymentMethod (camelCase) → guard always undefined, so COD orders also got payment rows via env strategy — now reads order.paymentMethod, skips COD, and warn-skips if field missing; tsc + eslint clean; E2E (env=vnpay throughout): single-seller zalopay order 90 → qcgateway.zalopay.vn URL ✅, single-seller vnpay order 91 → sandbox.vnpayment.vn URL ✅, multi-seller zalopay orders 92+93 → synchronous zalopay paymentUrl via TCP ✅, COD order 94 → payment-url {null,null} (no payment row) ✅; all test orders canceled, stock restored

- GHN shipping fee + cancel integration: ORDER_MESSAGE_PATTERN.CALCULATE_SHIPPING_FEE added; GhnService refactored with buildHeaders/buildShippingOrderBody helpers + previewShippingFee() (POST /v2/shipping-order/preview → {shippingFee, expectedDeliveryTime}) + cancelShippingOrder() (POST /v2/switch-status/cancel); Order entity adds shipping_fee DECIMAL(12,2) NULL column (synchronize applied); placeOrder + placeMultiSellerOrder compute fee via getShippingFeeOrZero (non-fatal, defaults 0 on GHN failure; multi-seller computes per-seller fee BEFORE the transaction so HTTP never holds a DB lock) and persist shippingFee, total = itemsTotal + shippingFee, codAmount includes fee; cancelOrder pushes cancel to GHN when ghnOrderCode exists (non-fatal); gateway adds POST /api/order/shipping-fee (JWT, ShippingFeeDto) → TCP CALCULATE_SHIPPING_FEE; BUG FIXED during E2E: placeOrder never set sellerId → "Field 'seller_id' doesn't have a default value" on every single-seller order (pre-existing since seller_id column added) — now derives sellerId from gateway-enriched items[0].sellerId, throws 400 if missing; tsc + eslint clean; E2E: fee preview Hà Nội 46207đ / HCM intra-city 0đ ✅, single-seller COD order 87 total 245207 = 199000+46207, shippingFee persisted, GHN order LXTCRV created ✅, cancel → local canceled + GHN status "cancel" verified via detail API + reserved stock released ✅, multi-seller COD orders 88+89 each carry own 46207 fee + own GHN code ✅ (test orders canceled after)

- Reserved-stock consume on delivery: orders.service.ts handleGhnWebhook now loads order items and, on first transition to COMPLETED (wasCompleted guard → duplicate "delivered" webhooks are no-ops), calls TCP INVENTORY_CONSUME_RESERVED_STOCK per item (SKU-scoped, non-fatal warn on failure) — full lifecycle is now reserve (TCP at creation) → release (ORDER_CANCELED consumer) XOR consume (GHN delivered → COMPLETED); chose webhook over PAYMENT_COMPLETED because non-COD orders at PROCESSING can still be canceled, while COMPLETED orders cannot → consume never races with release; tsc + eslint clean; E2E: reserve 1 unit of product 23 → webhook delivered for order 68 (real GHN code) → status completed + reserved 1→0 + available unchanged ✅, duplicate webhook → no change ✅, cancel completed order → 400 ✅

- Stock double-decrement fix: inventory handleOrderCreated (ORDER_CREATED_EVENT consumer) no longer calls reserveStock + consumeReservedStock — stock is reserved exactly once via TCP INVENTORY_RESERVE_STOCK by the orders service at creation; consumer is now ack-only (kept so the event doesn't go unhandled on the inventory queue); tsc + eslint clean; E2E: stock 10, multi-seller order qty 5 → available 5 / reserved 5 (was 0/5 before fix), cancel both child orders → available 10 / reserved 0, no permanent loss ✅; follow-up gap recorded in Known Issues (reserved stock never consumed on completion)

- Multi-seller stock pre-check: placeMultiSellerOrder (orders.service.ts) now runs the same INVENTORY_CHECK_STOCK loop as placeOrder before the order-creation transaction — SKU-aware ({productId, quantity, skuId}), throws BadRequestException on insufficient stock so no orders/payment are created; tsc + eslint clean; E2E: out-of-stock multi-seller COD order → 400 "Insufficient stock for product 10: requested 5, available 0", order count unchanged ✅; after stocking 10 units each → 201, 2 child orders (sellers 20+21), reservedStock +5 per product ✅; residual race between check and reserve still exists (reserve failure after commit only logs warning) — accepted for now

- Multi-order payment-url lookup fix: payments.service.ts getPaymentUrl() now falls back to JSONB containment query (order_ids @> CAST(:ids AS jsonb), ordered by created_at DESC) when the single order_id lookup misses — covers multi-order payments where order_id is NULL and IDs live in order_ids; tsc + eslint clean; E2E: multi-seller VNPay order (2 sellers → orders 79+80 + 1 multi-order payment) → GET /order/:id/payment-url returned 200 with orderUrl+status for both child orders ✅; fallback SQL verified executable (nonexistent ID → 200 {null,null}, no 500) ✅; discovery: ORDER_CREATED_EVENT consumer in payments also creates an individual payment row per non-COD child order, so the primary lookup usually hits — the JSONB fallback covers the case where RMQ event processing was unavailable at order time

- Multi-order payment gateway wiring: gateway order.service.ts — after CREATE_MULTI_SELLER_ORDER returns Order[], COD guard skips payment and returns { orders, paymentUrl: null }; non-COD collects orderIds + sums totalAmount, calls INITIATE_MULTI_ORDER_PAYMENT via TCP to payments service, returns { orders, paymentUrl }; PaymentMethod enum imported from @app/common for type-safe COD comparison; single-seller path unchanged; tsc + eslint clean

- Multi-order payment support: INITIATE_MULTI_ORDER_PAYMENT TCP pattern added to libs/constant; payment.entity.ts — orderId made nullable, unique constraint removed, orderIds JSONB column added; payments.service.ts adds processMultiOrderPayment() (creates one record, sets orderIds, generates payment URL via existing strategy); callback handlers (ZaloPay + VNPay) refactored to emit PAYMENT_COMPLETED_EVENT once per order ID (reads orderIds if present, falls back to [orderId]); migration SQL: database/multi_order_payment_migration.sql (drops unique on order_id, makes nullable, adds order_ids JSONB); tsc clean, eslint clean

- Product Review feature (Phases 0–4 complete): migration SQL (database/create_product_reviews_table.sql, no FK); constants VERIFY_PRODUCT_PURCHASED + REVIEW_CREATE/DELETE/FIND_BY_PRODUCT; orders service verifyUserPurchasedProduct (JOIN completed orders); product service ProductReview entity + createReview (ER_DUP_ENTRY → 409) + deleteReview + findReviewsByProduct + recalculateProductRating; gateway review.dto.ts + 3 endpoints (GET :id/reviews @Public, POST :id/reviews JWT, DELETE reviews/:reviewId JWT); bug fixed: REVIEW_DELETE handler returned void → 502, changed to return null; E2E 10/10 passed ✅

## Resolved Audit (2026-06-19)

Static review across gateway, user, product, inventory, orders, payments, rewards, notification, social, and chat. Critical findings were resolved. Validation at audit time: `npx tsc --noEmit` passed with zero errors; ESLint had 6 formatting errors + 7 warnings in files not touched by the read-only audit; runtime flows were not executed (stack not started). Remaining unresolved medium-severity gaps were promoted into `snapshot.md` → Known Issues.
